# The Azure App Service deploy (deploy_azure, ase-site-gated in
# rg-ase-webpage) was decommissioned — the app now runs on OpenShift
# (trainingcamp-prod, cluster ocp03). See CLAUDE.md §1b for the deploy
# runbook (manual `oc` commands, no CI wired up yet).
