One-sentence: Breadcrumb trail with caret separators; the last item renders as the current page.

```jsx
<Breadcrumb items={[{label:'Group',href:'#'},{label:'Responsibility',href:'#'},'Climate']} />
```
