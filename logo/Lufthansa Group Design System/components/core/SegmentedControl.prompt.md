One-sentence: Pill segmented control for 2–4 mutually-exclusive options; active segment lifts to a white pill.

```jsx
<SegmentedControl options={["One way","Return","Multi-city"]} value={trip} onChange={setTrip} />
```
Sizes `sm | md`. Use for trip type, view toggles, period switches.
