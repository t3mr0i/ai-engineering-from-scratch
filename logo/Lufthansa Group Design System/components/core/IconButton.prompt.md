One-sentence: Icon-only round button (Phosphor light glyph) for toolbars, nav and search.

```jsx
<IconButton icon="ph-magnifying-glass" label="Search" />
<IconButton icon="ph-bell" label="Notifications" variant="outline" />
```

Variants `ghost | solid | outline`, sizes `sm | md | lg`. Always pass `label` for accessibility.
