One-sentence: Radio group with the brand-blue selected dot.

```jsx
<RadioGroup name="seat" value={seat} onChange={setSeat}
  options={["Window","Aisle","No preference"]} />
```
`direction="row"` for inline. Accepts strings or `{value,label}`.
