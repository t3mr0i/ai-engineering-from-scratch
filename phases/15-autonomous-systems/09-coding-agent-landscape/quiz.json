{
  "lesson": "09-coding-agent-landscape",
  "title": "The Autonomous Coding Agent Landscape (2026)",
  "questions": [
    {
      "stage": "pre",
      "question": "A coding agent edits files before ever running the failing tests. What loop order fixes that?",
      "options": [
        "Edit more files faster",
        "Skip tests to save time",
        "Trust edits without verification",
        "Reproduce, fix boundedly, re-run green"
      ],
      "correct": 3,
      "explanation": "Edits without failing tests are guesses. This lesson scaffolds test-first agent loops."
    },
    {
      "stage": "check",
      "question": "What does CodeActScaffold.worst_touched measure for the three-bug fixture?",
      "options": [
        "The number of test assertions",
        "The largest number of files one snippet touched, which is 3",
        "The number of turns used by JSON",
        "The number of files in the initial repository"
      ],
      "correct": 1,
      "explanation": "The first CodeAct edit fixes app.py, util.py, and cli.py in one snippet, so worst_touched becomes 3."
    },
    {
      "stage": "check",
      "question": "What are the reported turn counts when both scaffolds use their default max_turns?",
      "options": [
        "JSON uses 2 and CodeAct uses 4",
        "Both use 3",
        "JSON uses 4 and CodeAct uses 2",
        "JSON uses 10 and CodeAct uses 10"
      ],
      "correct": 2,
      "explanation": "JSON fixes one failing path per turn and then needs a done turn (4 total); CodeAct fixes all three at once and then needs done (2 total)."
    },
    {
      "stage": "check",
      "question": "Why is the comparison meaningful even though both scaffolds use the same FIXES table?",
      "options": [
        "The tests are intentionally different for each scaffold",
        "CodeAct never runs tests",
        "The JSON scaffold has no blast radius",
        "The stub model and fixture are shared, so turn and action-shape differences come from the scaffold"
      ],
      "correct": 3,
      "explanation": "Both paths run the same deterministic run_tests and _apply_fix logic; the orchestration changes how many files an action can touch."
    },
    {
      "stage": "post",
      "question": "What trade-off does the demo attach to CodeAct's shorter run?",
      "options": [
        "A single snippet can touch more files, so sandbox isolation must control its larger blast radius",
        "It cannot pass the same tests",
        "It always needs more turns than JSON",
        "It removes the repository's test suite"
      ],
      "correct": 0,
      "explanation": "CodeAct compresses three edits into one action (two turns total), but that action has an observed blast radius of three files."
    },
    {
      "stage": "post",
      "question": "Which initial source change fixes the app.py fixture?",
      "options": [
        "Replace VERSION v0.0 with v1.0",
        "Replace return a - b with return a + b",
        "Replace s.upper() with s.lower()",
        "Add a fourth test to TESTS"
      ],
      "correct": 1,
      "explanation": "The first test expects add(2, 3) == 5, so the app.py rule changes the subtraction expression to addition."
    }
  ]
}
