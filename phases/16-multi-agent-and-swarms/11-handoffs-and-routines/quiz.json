{
  "lesson": "11-handoffs-and-routines",
  "title": "Handoffs and Routines — Stateless Orchestration",
  "questions": [
    {
      "stage": "pre",
      "question": "What event changes the active agent in this handoff miniature?",
      "options": [
        "A routing function returns an `Agent` object, which the run loop detects and installs as active.",
        "The current agent mutates a global state graph before every token.",
        "The manager broadcasts to every agent and waits for a majority vote.",
        "A fixed round counter selects the next agent without reading tool results."
      ],
      "correct": 0,
      "explanation": "`scripted_router` may return an `Agent`; `run_swarm` appends a handoff message, assigns that agent to `active`, and invokes it on the same user message."
    },
    {
      "stage": "check",
      "question": "What response does the refund flow produce for `\"I need a refund on order 77\"`?",
      "options": [
        "Quote for enterprise plan: $99/mo.",
        "Refund processed for order 77.",
        "Ticket opened for: I need a refund on order 77",
        "Could you tell me what you need help with?"
      ],
      "correct": 1,
      "explanation": "Triage sees `refund`, transfers to the refund agent, and that agent extracts the numeric order id `77` before calling `process_refund`."
    },
    {
      "stage": "check",
      "question": "What does the triage router return for the ambiguous message `\"hello\"`?",
      "options": [
        "A transfer to the support agent.",
        "A refund for the default order 42.",
        "The text `Could you tell me what you need help with?`.",
        "A sales quote for the enterprise plan."
      ],
      "correct": 2,
      "explanation": "No refund, buying/price, or broken/bug keyword matches. The triage branch therefore returns the clarification string rather than an `Agent`."
    },
    {
      "stage": "check",
      "question": "What is appended when triage hands a refund request to `refund_agent`?",
      "options": [
        "Only the final refund text; handoffs are invisible in history.",
        "A user message addressed to the manager.",
        "A reviewer verdict with the original prompt removed.",
        "An assistant `Msg` with content `(handoff to refund)` and sender `triage`, followed by the refund response."
      ],
      "correct": 3,
      "explanation": "`run_swarm` records the handoff as an assistant message before switching `active`; it then calls the refund agent and records its response with sender `refund`."
    },
    {
      "stage": "post",
      "question": "What does this lesson mean by “stateless orchestration”?",
      "options": [
        "Routing is encoded in agent instructions and tool-returned handoffs; the framework only switches on an `Agent` return.",
        "All conversations are persisted in a relational database by the framework.",
        "No message history is ever passed to an agent.",
        "Every handoff must be represented as a declarative graph edge."
      ],
      "correct": 0,
      "explanation": "The miniature has no state-machine DSL or graph runtime. Its orchestration primitive is a function returning the next `Agent`; prompts and tools carry the routing logic."
    },
    {
      "stage": "post",
      "question": "Which output belongs to the support branch for `\"my dashboard is broken\"`?",
      "options": [
        "Refund processed for order 42.",
        "Ticket opened for: my dashboard is broken",
        "Quote for enterprise plan: $99/mo.",
        "Could you tell me what you need help with?"
      ],
      "correct": 1,
      "explanation": "The triage router matches `broken` or `bug`, transfers to support, and `open_ticket` returns the issue prefixed with `Ticket opened for:`."
    }
  ]
}
