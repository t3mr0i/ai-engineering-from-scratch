# Guided demo: Policy Gradient — REINFORCE from Scratch

> **Time:** 10–15 minutes · **Question:** What observable evidence shows that you can formulate Policy Gradient — REINFORCE from Scratch in terms of states, actions, rewards, and objectives?

## Before you run

Write one predicted invariant for the baseline. Tie it to this objective: **Formulate Policy Gradient — REINFORCE from Scratch in terms of states, actions, rewards, and objectives.** Do not inspect the output first.

## Run the baseline

From the repository root:

```bash
python3 phases/09-reinforcement-learning/06-policy-gradients-reinforce/code/main.py
```

The command must print a bounded result and exit with status 0. Locate the part of the output that provides evidence for **Formulate Policy Gradient — REINFORCE from Scratch in terms of states, actions, rewards, and objectives**. Record the exact input, the relevant output, and the invariant in one sentence.

## Change one variable

Change the smallest input or configuration value that helps you investigate **Implement the central update rule from first principles**. Keep every other value fixed. Run the same command again and capture a before/after pair; a screenshot without the values is not sufficient evidence.

## Probe a failure

Choose an edge case or violated precondition related to **Trace learning signals through a self-terminating experiment**. Predict whether the program should reject it, degrade gracefully, or return a different valid result. Run the probe and explain any mismatch between prediction and observation. Restore the source afterward.

## Exit ticket

In three sentences, state (1) the mechanism you observed, (2) the controlled change and its effect, and (3) the acceptance check that demonstrates you can **evaluate convergence, stability, and exploration trade-offs**. If the evidence is ambiguous, name the next measurement rather than claiming success.

