{
  "lesson": "80-checkpoint-sharded-resume",
  "title": "Sharded Checkpoint and Atomic Resume",
  "questions": [
    {
      "stage": "pre",
      "question": "A multi-file artifact publishes while readers watch, and some readers see half-written files. What ordering fixes that?",
      "options": [
        "Publish the manifest first, files later",
        "Delete old files before writing new ones",
        "Let readers retry until files look complete",
        "Write and sync shards first, replace the manifest last"
      ],
      "correct": 3,
      "explanation": "Readers trust the manifest. This lesson publishes shards first and swaps the manifest atomically last."
    },
    {
      "stage": "check",
      "question": "What does load_sharded do when the manifest world_size differs from the requested world_size?",
      "options": [
        "It silently maps the old shards onto the new ranks",
        "It pads missing ranks with zero tensors",
        "It loads only rank 0 and ignores the mismatch",
        "It raises CheckpointError before returning per-rank state"
      ],
      "correct": 3,
      "explanation": "World size is part of the checkpoint contract. load_sharded validates it, along with schema and shard metadata, and rejects a mismatch rather than guessing a remapping."
    },
    {
      "stage": "check",
      "question": "What does a SHA-256 mismatch tell load_sharded about a shard file?",
      "options": [
        "The file was altered or corrupted after the manifest was written, so loading fails",
        "The loader should recalculate and accept the new digest",
        "Only the shard's wall-clock metadata is stale",
        "The file is valid as long as its size is unchanged"
      ],
      "correct": 0,
      "explanation": "The manifest records each shard digest. load_sharded hashes the bytes and raises when the digest differs, catching tampering or corruption rather than silently resuming."
    },
    {
      "stage": "check",
      "question": "Which path is rejected by the checkpoint loader's shard-path validation?",
      "options": [
        "A simple relative name such as rank0.bin",
        "An absolute path or a name containing a path separator or '..' component",
        "A file whose digest matches the manifest",
        "A rank number that is present exactly once"
      ],
      "correct": 1,
      "explanation": "Shard names must be safe simple relative paths. Absolute names, separators, empty names, '.' and '..' are rejected before resolution to prevent escaping the checkpoint directory."
    },
    {
      "stage": "post",
      "question": "If seven step directories exist and rotate_checkpoints(parent, keep_last=3) runs, how many are removed?",
      "options": [
        "7, because rotation always starts from an empty directory",
        "3, leaving four for a safety margin",
        "4, leaving the three newest directories",
        "0, because keep_last is advisory only"
      ],
      "correct": 2,
      "explanation": "The function sorts checkpoint directories and keeps the newest three. The older seven-minus-three = four directories are removed."
    },
    {
      "stage": "post",
      "question": "Which fields identify the on-disk format in ShardManifest?",
      "options": [
        "Only step and wall_clock_seconds",
        "Model ID, task ID, and metric name",
        "Rank, stdout, and exit code",
        "world_size, step, wall_clock_seconds, shards, and schema_version=1"
      ],
      "correct": 3,
      "explanation": "The manifest serializes the checkpoint contract: world size and step, timing metadata, the ShardEntry list, and schema_version 1 for format validation."
    }
  ]
}
