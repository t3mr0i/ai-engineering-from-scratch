{
  "lesson": "62-vision-language-pretraining",
  "title": "Vision-Language Pretraining",
  "questions": [
    {
      "stage": "pre",
      "question": "What two distinct capabilities does combining InfoNCE and LM loss train into one model?",
      "options": [
        "Ranking (find the right image for a caption) and generation (write a caption for an image)",
        "Speed and accuracy",
        "Compression and decompression",
        "Tokenization and detokenization"
      ],
      "correct": 0,
      "explanation": "InfoNCE handles ranking; LM loss handles generation. CoCa, BLIP, and friends combine both in one training pass."
    },
    {
      "stage": "check",
      "question": "In InfoNCE for a batch of N pairs, how many positive pairs and negative pairs are formed?",
      "options": [
        "1 positive and N negatives",
        "N positives along the diagonal of the similarity matrix and N*(N-1) negatives off-diagonal",
        "N positives and N negatives",
        "N^2 positives"
      ],
      "correct": 1,
      "explanation": "The N matching pairs sit on the diagonal; everything off-diagonal is a negative."
    },
    {
      "stage": "check",
      "question": "In info_nce_loss, what does log_tau.exp().clamp(1e-3, 100.0) do to the normalized dot products?",
      "options": [
        "It turns the similarity matrix into hard one-hot labels",
        "It masks all off-diagonal pairs",
        "It supplies a bounded positive scale for the similarity logits; log_tau remains trainable",
        "It converts image embeddings into token IDs"
      ],
      "correct": 2,
      "explanation": "The code exponentiates the trainable log_tau and clamps the resulting scale before multiplying img_n @ txt_n.T. The diagonal and off-diagonal logits are all scaled; only the cross-entropy targets select the diagonal."
    },
    {
      "stage": "check",
      "question": "Which sub-modules receive gradient from the LM loss but not from the contrastive loss?",
      "options": [
        "The vision encoder",
        "The MLP projector",
        "The text-side encoder",
        "The cross-attention decoder"
      ],
      "correct": 3,
      "explanation": "Contrastive loss does not touch the decoder; LM loss does not touch the text-side mean-pool encoder."
    },
    {
      "stage": "post",
      "question": "What does ignore_index=PAD_ID accomplish in the LM cross-entropy?",
      "options": [
        "Masks padding positions out of the loss so the model is not punished for predicting tokens after the caption ends",
        "Speeds up the loss",
        "Encrypts pad tokens",
        "Reorders tokens"
      ],
      "correct": 0,
      "explanation": "Padding positions carry no signal; ignoring them stops the loss from learning to predict pad."
    },
    {
      "stage": "post",
      "question": "What does the 50-step synthetic run actually establish?",
      "options": [
        "It proves the model has production-level caption quality",
        "It proves the configured encoder, projector, and decoder losses can backpropagate on the fixture; it is not a quality result on real data",
        "It proves 50 steps is a universal training budget",
        "It proves real images would produce the same metrics"
      ],
      "correct": 1,
      "explanation": "The demo uses a deterministic 200-pair synthetic corpus and checks that the loss path runs and moves. That is useful plumbing evidence, not evidence about generalization or real-data quality."
    }
  ]
}
