{
  "lesson": "36-training-loop-eval",
  "title": "Training Loop and Evaluation",
  "questions": [
    {
      "stage": "pre",
      "question": "Your team trains a small model in steps and wants to catch problems early without fooling yourselves. Which habit from ordinary model training helps most?",
      "options": [
        "Train on all available data and never check anything separately.",
        "Track loss on data the model is not trained on, save the loss history, and look at real samples during training.",
        "Stop saving any history so the disk stays empty.",
        "Judge training only by how fast each step runs, ignoring what the model produces."
      ],
      "correct": 1,
      "explanation": "That habit of held-out checks plus saved histories and samples prepares you for this lesson, which assembles the full training loop with scheduled step sizes and regular evaluation."
    },
    {
      "stage": "check",
      "question": "For a 1D token tensor of length `N` and context length `T`, what value does `make_batches` assign to `max_start`?",
      "options": [
        "`N - T + 1`",
        "`N - T`",
        "`T - N - 1`",
        "`N - T - 1`"
      ],
      "correct": 3,
      "explanation": "The implementation sets `max_start = token_ids.numel() - context_length - 1`. The extra one is needed because each input window needs a shifted target token."
    },
    {
      "stage": "check",
      "question": "Which parameters does `build_param_groups` place in the no-decay group?",
      "options": [
        "Biases and LayerNorm-style `shift`/`scale` parameters, plus tensors with fewer than two dimensions",
        "Only parameters whose names contain `weight`",
        "All matrices and embedding weights",
        "Only the final language-model head"
      ],
      "correct": 0,
      "explanation": "The grouping excludes bias, shift, and scale names from weight decay and also excludes parameters with dimension below two. Matrix and embedding weights go into the decayed group."
    },
    {
      "stage": "check",
      "question": "With `max_lr=3e-3` and `warmup_steps=10`, what learning rate does `cosine_with_warmup(step=0)` return?",
      "options": [
        "0",
        "0.0003",
        "0.003",
        "0.03"
      ],
      "correct": 1,
      "explanation": "During warmup the function uses `max_lr * (step + 1) / warmup_steps`, so step zero is `0.003 * 1/10 = 0.0003`."
    },
    {
      "stage": "post",
      "question": "What does `evaluate_model` do after computing its mean loss?",
      "options": [
        "It leaves the model in evaluation mode permanently.",
        "It performs an optimizer step on the validation batches.",
        "It restores training mode if the model was training before evaluation.",
        "It returns only the last batch's loss."
      ],
      "correct": 2,
      "explanation": "Evaluation runs under `torch.no_grad()` and averages up to `max_batches`. It records the prior `model.training` state and calls `model.train()` before returning when necessary."
    },
    {
      "stage": "post",
      "question": "When does the training loop write an evaluation and sample probe with `eval_every=20`?",
      "options": [
        "Only at step zero",
        "At every step, regardless of the interval",
        "Only after training has already finished",
        "When `(step + 1) % 20 == 0`, and also on the final step"
      ],
      "correct": 3,
      "explanation": "The loop triggers the eval/sample branch whenever the one-based step count is divisible by eval_every or the current step is the final one, and logs those events as JSONL records."
    }
  ]
}
