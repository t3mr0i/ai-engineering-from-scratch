{
  "lesson": "78-zero-parameter-sharding",
  "title": "ZeRO Optimizer State Sharding",
  "questions": [
    {
      "stage": "pre",
      "question": "What does this ZeRO-1 implementation shard across ranks?",
      "options": [
        "The model parameters themselves, which are absent on non-owning ranks",
        "Only the input batch, while optimizer state stays replicated",
        "The fp32 master parameters and Adam m/v state; the full model parameters remain present",
        "Activations only; all optimizer tensors remain full-sized"
      ],
      "correct": 2,
      "explanation": "This is optimizer-state sharding: every module still holds full model parameters, while each rank owns one shard of the fp32 master copy and Adam moments."
    },
    {
      "stage": "check",
      "question": "For total=17, world_size=4, and rank=3, what does shard_bounds return?",
      "options": [
        "(12, 17, 4)",
        "(13, 17, 5)",
        "(15, 20, 5)",
        "(15, 17, 5)"
      ],
      "correct": 3,
      "explanation": "The padded chunk size is ceil(17/4)=5. Rank 3 starts at 15 and has a logical end min(20,17)=17, so the tuple is (15,17,5)."
    },
    {
      "stage": "check",
      "question": "Which sequence describes ZeroOptimizer.step after gradients are available?",
      "options": [
        "Reduce-scatter summed gradients, divide by world_size, update the local Adam shard, then all-gather parameters",
        "All-gather parameters, update every full Adam state, then discard gradients",
        "Update only rank 0 and broadcast its loss",
        "Reduce-scatter parameters and never reconstruct the full model"
      ],
      "correct": 0,
      "explanation": "The step flattens gradients, reduce-scatters their sums and averages, applies bias-corrected Adam to each local shard, then all-gathers master shards back into full parameters."
    },
    {
      "stage": "check",
      "question": "What is the role of padding in a non-divisible flattened parameter vector?",
      "options": [
        "It changes the model's parameter count permanently",
        "It makes equal communication chunks; the padded tail is removed when real parameters are scattered back",
        "It drops the last real parameters to preserve equal chunks",
        "It allows ranks to use different optimizer learning rates"
      ],
      "correct": 1,
      "explanation": "Padding is only a communication layout convenience. The extra slots are zeros and are sliced away when the original-length parameter vector is reconstructed."
    },
    {
      "stage": "post",
      "question": "For one rank's optimizer state, which storage is represented by shard_bytes?",
      "options": [
        "Only the model's fp16 activations",
        "The full model parameter vector plus one gradient buffer",
        "One fp32 master shard plus its Adam m and v shards",
        "The complete master, m, and v vectors replicated on every rank"
      ],
      "correct": 2,
      "explanation": "The memory table counts three fp32 arrays for the local chunk: master parameters, first moment, and second moment. It does not claim that all optimizer state is local."
    },
    {
      "stage": "post",
      "question": "How does gather_flat_grads represent a parameter whose grad is None?",
      "options": [
        "It skips the parameter and shifts every later offset",
        "It raises because every parameter must receive a gradient",
        "It copies the previous parameter's gradient",
        "It inserts a zero tensor with the same shape before flattening"
      ],
      "correct": 3,
      "explanation": "A missing gradient contributes zeros of the parameter's shape, preserving a stable flat layout for reduce-scatter and later parameter updates."
    }
  ]
}
