# Guided demo: Capstone 01 — Terminal-Native Coding Agent

> **Time:** 10–15 minutes · **Question:** What observable evidence shows that you can define measurable acceptance criteria for Capstone 01 — Terminal-Native Coding Agent?

## Before you run

Write one predicted invariant for the baseline. Tie it to this objective: **Define measurable acceptance criteria for Capstone 01 — Terminal-Native Coding Agent.** Do not inspect the output first.

## Run the baseline

From the repository root:

```bash
python3 phases/19-capstone-projects/01-terminal-native-coding-agent/code/main.py
```

The command must print a bounded result and exit with status 0. Locate the part of the output that provides evidence for **Define measurable acceptance criteria for Capstone 01 — Terminal-Native Coding Agent**. Record the exact input, the relevant output, and the invariant in one sentence.

## Change one variable

Change the smallest input or configuration value that helps you investigate **Integrate the required components into one self-terminating workflow**. Keep every other value fixed. Run the same command again and capture a before/after pair; a screenshot without the values is not sufficient evidence.

## Probe a failure

Choose an edge case or violated precondition related to **Exercise happy paths, edge cases, and failure recovery with reproducible fixtures**. Predict whether the program should reject it, degrade gracefully, or return a different valid result. Run the probe and explain any mismatch between prediction and observation. Restore the source afterward.

## Exit ticket

In three sentences, state (1) the mechanism you observed, (2) the controlled change and its effect, and (3) the acceptance check that demonstrates you can **package the verified result as a reusable curriculum artifact**. If the evidence is ambiguous, name the next measurement rather than claiming success.

