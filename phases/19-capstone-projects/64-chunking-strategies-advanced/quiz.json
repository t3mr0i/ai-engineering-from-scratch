{
  "lesson": "64-chunking-strategies-advanced",
  "title": "Chunking Strategies, Compared",
  "questions": [
    {
      "stage": "pre",
      "question": "Why is the chunking choice an upper bound on retrieval quality and not just a hyperparameter?",
      "options": [
        "Because a split can leave no candidate with coherent answer evidence, making retrieval and ranking less reliable and leaving downstream models without context to compensate consistently",
        "Because the LLM never reads chunks",
        "Because the embedding model is fixed at training time",
        "Because BM25 has no role in modern RAG"
      ],
      "correct": 0,
      "explanation": "Chunking sets an evidence ceiling: when no retrieved candidate contains the answer span coherently, embeddings and rerankers have less usable context to rank, and downstream models cannot reliably compensate for missing evidence. Neighboring chunks or reconstruction can help in some cases, so this is a limit rather than an absolute impossibility."
    },
    {
      "stage": "check",
      "question": "Which chunker follows a separator hierarchy and falls back to finer separators when a region is too long?",
      "options": [
        "fixed_window",
        "recursive_split",
        "sentence_chunks",
        "semantic_chunks"
      ],
      "correct": 1,
      "explanation": "recursive_split tries the configured separators from coarse to fine, recursively splitting oversized pieces until they fit the target. The other strategies use different boundary rules."
    },
    {
      "stage": "check",
      "question": "What does recall@k measure in this lesson?",
      "options": [
        "Whether the top-k chunks contain the literal query string",
        "The cosine similarity between the query and the top-k chunks",
        "Whether any of the top-k chunks overlaps the gold answer span",
        "The number of unique documents in the top-k"
      ],
      "correct": 2,
      "explanation": "Each query carries gold offsets; recall@k is 1 if any top-k chunk overlaps the gold span, else 0; averaged across queries."
    },
    {
      "stage": "check",
      "question": "Why can fixed_window split code or YAML meaning?",
      "options": [
        "It removes all Unicode characters before indexing",
        "It always creates chunks longer than the embedding context",
        "It cannot be run in parallel",
        "Character boundaries can separate an identifier or key/value pair, so no chunk contains the complete unit"
      ],
      "correct": 3,
      "explanation": "fixed_window slices raw character offsets without knowing syntax. A cut inside a symbol or mapping can leave retrieval with only a fragment of the fact it needs."
    },
    {
      "stage": "post",
      "question": "What is the centroid drift termination criterion in semantic chunking?",
      "options": [
        "Cosine similarity between the running mean vector and the next sentence drops below a threshold",
        "Character count exceeds the max",
        "Sentence count exceeds a hardcoded limit",
        "The chunker has consumed the entire embedding budget"
      ],
      "correct": 0,
      "explanation": "Semantic chunking cuts when the running centroid no longer matches the next sentence by a configured similarity threshold."
    },
    {
      "stage": "post",
      "question": "What stale-embedding failure mode does semantic chunking introduce that the other four strategies do not?",
      "options": [
        "Chunks too small for the embedding context",
        "Chunk boundaries change whenever you change the chunk-model, requiring a coordinated rebuild",
        "Chunks are no longer indexable by BM25",
        "Chunks lose their start and end offsets"
      ],
      "correct": 1,
      "explanation": "Semantic clustering depends on an embedding model; swapping the model changes the boundaries, so the chunk-model must be pinned separately or rebuilt together with the retrieval model."
    }
  ]
}
