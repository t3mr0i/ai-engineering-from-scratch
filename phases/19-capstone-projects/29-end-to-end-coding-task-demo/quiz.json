{
  "lesson": "29-end-to-end-coding-task-demo",
  "title": "End-to-End Coding Agent on the Harness",
  "questions": [
    {
      "stage": "pre",
      "question": "Before reading the lesson: why does the end-to-end demo replace the LLM with a deterministic policy?",
      "options": [
        "The harness contract is what the lesson is testing; the policy is the substitutable seam",
        "The LLM would take too many wall-clock seconds to complete inside the test suite's CI timeout",
        "A deterministic policy always outperforms a real LLM on this specific coding fixture's bug",
        "Running a real LLM for every test invocation would exceed the lesson's inference budget"
      ],
      "correct": 0,
      "explanation": "The harness contract does not care whether the policy is an LLM or a state machine. Removing the LLM removes the network and the variance and turns the lesson into a reproducible integration test — speed, accuracy, and cost are not the reason."
    },
    {
      "stage": "check",
      "question": "The agent's state machine has five states. In what order does it execute them on a normal pass?",
      "options": [
        "SURVEY -> FIX -> VERIFY -> RUN_TESTS -> INSPECT.",
        "RUN_TESTS -> SURVEY -> INSPECT -> FIX -> VERIFY.",
        "SURVEY -> INSPECT -> FIX -> RUN_TESTS -> VERIFY.",
        "SURVEY -> RUN_TESTS -> INSPECT -> FIX -> VERIFY."
      ],
      "correct": 3,
      "explanation": "The agent surveys the project, runs the tests, inspects the failing test, fixes the bug, then verifies. RUN_TESTS comes before INSPECT because the test failure is the signal the policy reads."
    },
    {
      "stage": "check",
      "question": "The bundled fixture's bug is an off-by-one in src/fizz.py: range(1, n) instead of range(1, n + 1). Where does the policy decide to apply the fix?",
      "options": [
        "Inside the SURVEY state, immediately after reading the file header for src/fizz.py",
        "Inside the INSPECT state, right after the failing test names the expected output",
        "Inside the VERIFY state, only after the test suite has been rerun once already",
        "Outside the state machine entirely, inside the gate chain that wraps every tool call"
      ],
      "correct": 1,
      "explanation": "INSPECT is where the policy reads the failing test and learns the expected shape. The fix is identified there and written in the next FIX state."
    },
    {
      "stage": "check",
      "question": "Every tool call goes through what chain of components in order?",
      "options": [
        "Sandbox -> GateChain -> SpanBuilder -> ObservationLedger.",
        "SpanBuilder -> GateChain -> Sandbox -> ObservationLedger.",
        "GateChain -> SpanBuilder -> Sandbox -> ObservationLedger.",
        "ObservationLedger -> GateChain -> Sandbox -> SpanBuilder."
      ],
      "correct": 2,
      "explanation": "Gate first (allow / refuse), span next (so the refusal is also captured), sandbox third (only if allowed), ledger last (only on success). The lesson code wires it that way."
    },
    {
      "stage": "post",
      "question": "You replace the deterministic policy with a real LLM. Which harness component changes?",
      "options": [
        "The gate chain has to be rewritten from scratch to handle the stochastic, non-deterministic nature of LLM output",
        "The sandbox has to be hardened with additional seccomp filtering rules before an LLM-driven agent can run safely",
        "The Prometheus exposition format becomes incompatible once span attributes start coming from a real LLM policy",
        "Nothing in the harness changes; the policy is the only piece that swaps, since the seam is policy-agnostic"
      ],
      "correct": 3,
      "explanation": "The lesson's point: the harness is policy-agnostic. The seam is the next_action / observe interface. The LLM plugs in there without touching gate, sandbox, ledger, or spans."
    },
    {
      "stage": "post",
      "question": "The demo asserts the agent solves the fixture in fewer than 12 steps. The deterministic policy uses 5. What is a real-LLM agent likely to do, and how does the harness handle it?",
      "options": [
        "The LLM may need more steps; step_budget bounds the loop, and an unsolved run fails with a halted_reason",
        "The LLM will also converge on exactly 5 steps, so the harness behavior stays unchanged either way",
        "A real LLM cannot be plugged into this particular harness implementation for any reason at all",
        "The harness silently extends the step budget whenever the policy has not yet reached VERIFY"
      ],
      "correct": 0,
      "explanation": "The step budget is the loud, deterministic stopping criterion. An LLM that wanders gets capped and the report tells you exactly why."
    }
  ]
}
