{
  "lesson": "79-pipeline-parallel",
  "title": "Pipeline Parallel and Bubble Analysis",
  "questions": [
    {
      "stage": "pre",
      "question": "What bubble fraction does bubble_fraction(num_stages=4, num_microbatches=8) return?",
      "options": [
        "3/11, about 0.273",
        "4/8, exactly 0.5",
        "1/4, because there are four stages",
        "8/11, because forward work dominates"
      ],
      "correct": 0,
      "explanation": "The closed form is (n-1)/(m+n-1). With n=4 and m=8, it is 3/(8+4-1)=3/11, about 27.3%."
    },
    {
      "stage": "check",
      "question": "In gpipe_schedule, at what cycle is the forward unit for microbatch m on stage s placed?",
      "options": [
        "m - s",
        "m + s",
        "m * s",
        "m + num_stages - s"
      ],
      "correct": 1,
      "explanation": "The schedule emits forward work at cycle = microbatch + stage. This diagonal placement models the pipeline warm-up before the backward wave."
    },
    {
      "stage": "check",
      "question": "How does measure_bubble compute idle work in the generated schedule?",
      "options": [
        "It counts only backward units and calls the remainder communication",
        "It divides the number of stages by the number of microbatches",
        "It compares occupied cycle-stage slots with all slots through the maximum cycle",
        "It assumes every stage is busy for exactly m cycles"
      ],
      "correct": 2,
      "explanation": "measure_bubble forms a stage-by-cycle grid up to max_cycle+1, counts scheduled F/B units, and reports the unoccupied fraction of that grid."
    },
    {
      "stage": "check",
      "question": "Which stage shapes are used by the real two-rank demo?",
      "options": [
        "Rank 0: 16→4; rank 1: 4→16",
        "Both ranks: 16→32→4",
        "Rank 0: 32→16; rank 1: 16→8",
        "Rank 0: 16→32→16; rank 1: 16→32→4"
      ],
      "correct": 3,
      "explanation": "The demo's first stage maps the input through 16→32→16, while the second stage consumes 16 features and maps 16→32→4 for the final output."
    },
    {
      "stage": "post",
      "question": "What happens to the modeled bubble fraction as the number of microbatches grows with stages fixed?",
      "options": [
        "It shrinks toward zero because the fixed warm-up is amortized",
        "It grows linearly with every added microbatch",
        "It stays exactly one half for every schedule",
        "It becomes undefined once there are more microbatches than stages"
      ],
      "correct": 0,
      "explanation": "In (n-1)/(m+n-1), the numerator is fixed while m grows. More microbatches fill the pipeline and reduce the relative warm-up/flush idle fraction."
    },
    {
      "stage": "post",
      "question": "When does the schedule begin its backward wave relative to the forward wave?",
      "options": [
        "At cycle m, before the last forward reaches the final stage",
        "After forward_end = num_microbatches + num_stages - 1",
        "Immediately after the first forward unit on stage 0",
        "Only after an optimizer step has completed"
      ],
      "correct": 1,
      "explanation": "The schedule defines forward_end = m+n-1, then emits backward units in reverse stage order from that point. It is a schedule model; the real wire demo separately sends activations and gradients."
    }
  ]
}
