{
  "lesson": "43-hdf5-tokenized-corpus",
  "title": "HDF5 Tokenized Corpus",
  "questions": [
    {
      "stage": "pre",
      "question": "Why store the tokenized corpus in a resizable, chunked HDF5 dataset rather than JSONL?",
      "options": [
        "JSONL records are already fixed-size integer arrays, so seeking never scans text.",
        "HDF5 removes the need to track token counts or shard offsets.",
        "HDF5 stores integer tokens in chunks and supports addressable dataset slices; h5py serves those slices through its page cache into NumPy without reparsing JSONL.",
        "HDF5 guarantees that every sampled window is one physical disk read."
      ],
      "correct": 2,
      "explanation": "MmapTokenStore is a class name, not an operating-system mmap claim: it opens each shard with h5py.File(..., 'r', swmr=True) and copies dataset slices into NumPy. The benefit here is chunked integer storage plus addressable, page-cache-backed reads; chunk boundaries still affect I/O."
    },
    {
      "stage": "check",
      "question": "Why should a training window be considered when choosing the HDF5 chunk_size?",
      "options": [
        "The chunk size controls the tokenizer vocabulary, so it must be 257.",
        "A larger chunk automatically makes every random window contiguous across shards.",
        "A chunk must equal the document count or HDF5 cannot resize the dataset.",
        "Choosing a chunk size unrelated to the window forces many more chunk reads; a compatible multiple can keep most sampled windows in one or two chunks."
      ],
      "correct": 3,
      "explanation": "The writer stores contiguous token chunks, while SlidingWindowDataloader requests window_size + 1 tokens. Aligning their scales reduces boundary crossings, although it does not guarantee every random window is one chunk."
    },
    {
      "stage": "check",
      "question": "Why is tokens.attrs['token_count'] written when the HDF5 shard closes?",
      "options": [
        "It is the explicit logical length used in ShardWriteResult, index construction, and bounds checks; readers do not have to infer that contract from chunk settings.",
        "It pads the final HDF5 chunk with zeros so all shards have equal length.",
        "It stores the tokenizer vocabulary size so a reader can decode bytes.",
        "It records the number of JSONL lines even when a line produced no tokens."
      ],
      "correct": 0,
      "explanation": "The writer increments _token_count for flushed token data and stores that value as an attribute. The pipeline copies it into shards.json, where global offsets and slice bounds are computed."
    },
    {
      "stage": "check",
      "question": "What is the purpose of _flush_buffer in HDF5ShardWriter?",
      "options": [
        "It rewrites the whole shard after every document so no buffer remains in memory.",
        "It grows the resizable dataset with a contiguous buffered block and updates the hash/count, while the final call also writes any residual tokens.",
        "It changes byte IDs into BPE IDs before HDF5 sees them.",
        "It reserves a fixed file length and discards tokens beyond the first chunk."
      ],
      "correct": 1,
      "explanation": "Documents append to _buffer; full chunks are extended and written together, and __exit__ flushes a shorter final block. The same bytes update the running SHA-256 and token count."
    },
    {
      "stage": "post",
      "question": "Which SWMR open modes are used when a shard moves from writing to reading?",
      "options": [
        "The reader uses ordinary text mode and the writer reopens the completed file for appending.",
        "Both writer and reader open the completed shard with h5py.File(..., 'w') so they share the resize lock.",
        "The writer creates with h5py.File(..., 'w', libver='latest') and sets swmr_mode=True; MmapTokenStore later opens the finished shard read-only with swmr=True.",
        "SWMR is only a JSONL setting; HDF5 ignores the flag."
      ],
      "correct": 2,
      "explanation": "HDF5ShardWriter creates the resizable dataset with h5py.File(..., 'w', libver='latest') and enables self._file.swmr_mode while writing. After the writer closes the shard, MmapTokenStore opens it with h5py.File(entry.path, 'r', swmr=True)."
    },
    {
      "stage": "post",
      "question": "What does the boundary token id exist to make explicit?",
      "options": [
        "Vocabulary size limits",
        "Compression",
        "It assigns a separate vocabulary to each source document",
        "Document boundaries within a packed window so the model is not silently trained on cross-document concatenation"
      ],
      "correct": 3,
      "explanation": "Without an explicit boundary the trainer learns boundaries as noise; with it the boundary becomes a usable separator."
    }
  ]
}
