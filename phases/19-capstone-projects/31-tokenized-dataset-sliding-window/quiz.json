{
  "lesson": "31-tokenized-dataset-sliding-window",
  "title": "Tokenized Dataset with Sliding Windows",
  "questions": [
    {
      "stage": "pre",
      "question": "You have turned a long text into a stream of number IDs once, and you want many short training examples where the model guesses the next ID. What slicing idea from ordinary data preparation helps most?",
      "options": [
        "Use the whole stream as a single example so the model sees everything at once.",
        "Cut the stream into overlapping short chunks so each chunk gives many input and next-ID pairs.",
        "Shuffle the IDs into random order to destroy all word order before training.",
        "Convert the text to IDs again from scratch for every single example."
      ],
      "correct": 1,
      "explanation": "Seeing a long ID stream as a source of many overlapping next-ID examples prepares you for this lesson, which turns that idea into a reusable dataset with controlled overlap and repeatable shuffling."
    },
    {
      "stage": "check",
      "question": "With `num_ids=100`, `context_length=16`, and `stride=8`, how many windows does `count_windows` return?",
      "options": [
        "8",
        "10",
        "12",
        "11"
      ],
      "correct": 3,
      "explanation": "The usable start range has `100 - (16 + 1) = 83` tokens. The implementation computes `1 + 83 // 8 = 11` complete windows."
    },
    {
      "stage": "check",
      "question": "What stride does the dataset use when no stride is supplied?",
      "options": [
        "`context_length`",
        "`context_length + 1`",
        "One token",
        "Half the context length"
      ],
      "correct": 0,
      "explanation": "The constructor defaults stride to the context length, producing non-overlapping training windows unless a smaller stride is explicitly requested."
    },
    {
      "stage": "check",
      "question": "How does `make_dataloader` make shuffling repeatable across epochs?",
      "options": [
        "It uses the wall clock as a new seed each batch.",
        "It seeds a generator with `base_seed + epoch`.",
        "It disables shuffling after the first epoch.",
        "It hashes the vocabulary size but ignores the epoch."
      ],
      "correct": 1,
      "explanation": "The loader creates a generator and calls `manual_seed(base_seed + epoch)`, so the same epoch is reproducible while different epochs receive distinct deterministic orders."
    },
    {
      "stage": "post",
      "question": "With `context_length=16` and `stride=8`, how many tokens do adjacent windows share?",
      "options": [
        "0",
        "4",
        "8",
        "16"
      ],
      "correct": 2,
      "explanation": "Each training window spans 17 token IDs, and starts move forward by eight. Their input portions therefore overlap by `16 - 8 = 8` tokens."
    },
    {
      "stage": "post",
      "question": "What happens if `SlidingWindowDataset` receives `context_length=0`?",
      "options": [
        "It creates one-token examples.",
        "It silently changes the context to one.",
        "It returns an empty dataset without an error.",
        "It raises `ValueError` because context length must be positive."
      ],
      "correct": 3,
      "explanation": "The constructor validates context_length and rejects values below one. It also rejects empty token arrays and non-positive strides rather than creating ambiguous samples."
    }
  ]
}
