{
  "lesson": "73-ai-fundamentals",
  "title": "AI Fundamentals",
  "questions": [
    {
      "stage": "pre",
      "question": "A request for contract review could use a keyword rule, a classifier, a RAG generator, or an agent. What decides between them?",
      "options": [
        "Whichever demo looks most impressive",
        "The newest available model",
        "Whatever the team already knows",
        "Timeline, data needs, compliance burden, and cost per query"
      ],
      "correct": 3,
      "explanation": "Each layer implies different build cost and governance. This lesson classifies first so scoping matches the real need."
    },
    {
      "stage": "check",
      "question": "What type does classify_system_type return for the Document summariser profile with open-ended text and no live retrieval?",
      "options": [
        "RULE_BASED",
        "ML_CLASSIFIER",
        "AGENTIC",
        "GENERATIVE"
      ],
      "correct": 3,
      "explanation": "The profile produces open-ended content but does not act through a multi-step external workflow. Gate 2 returns GENERATIVE; the absence of live retrieval only changes the reasoning note."
    },
    {
      "stage": "check",
      "question": "The Contract risk flagger profile has bounded output and no external actions or multi-step plan. Which enum does this implementation return?",
      "options": [
        "ML_CLASSIFIER",
        "RULE_BASED",
        "GENERATIVE",
        "AGENTIC"
      ],
      "correct": 0,
      "explanation": "After rejecting agentic and generative paths, this implementation defaults bounded variable-input cases to ML_CLASSIFIER while recommending a deterministic rule when possible."
    },
    {
      "stage": "check",
      "question": "An AgentRiskProfile has blast_radius=IRREVERSIBLE and no human checkpoint. What verdict does evaluate_agent_risk return?",
      "options": [
        "PROCEED",
        "BLOCK",
        "REVIEW REQUIRED",
        "PROCEED if the prompt is short"
      ],
      "correct": 1,
      "explanation": "The first risk gate blocks irreversible actions without a technically enforced human checkpoint. Later reversibility or injection checks cannot weaken that decision."
    },
    {
      "stage": "post",
      "question": "A limited-write agent has all actions reversible, a human checkpoint, and no external attacker text. Which verdict follows?",
      "options": [
        "BLOCK",
        "REVIEW REQUIRED",
        "PROCEED",
        "GENERATIVE"
      ],
      "correct": 2,
      "explanation": "The blast radius is not irreversible, all actions are reversible, and no prompt-injection surface is declared. The evaluator reaches PROCEED after its gates."
    },
    {
      "stage": "post",
      "question": "A broad-write agent has reversible actions but reads external attacker text. What verdict does evaluate_agent_risk return?",
      "options": [
        "PROCEED",
        "BLOCK",
        "GENERATIVE",
        "REVIEW REQUIRED"
      ],
      "correct": 3,
      "explanation": "Because the actions are reversible, the broad-write non-reversibility branch does not fire. The external attacker-text gate still requires REVIEW before deployment."
    }
  ]
}
