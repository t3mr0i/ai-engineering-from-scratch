{
  "lesson": "11-caching-cost",
  "title": "Caching, Rate Limiting & Cost Optimization",
  "questions": [
    {
      "stage": "pre",
      "question": "Your assistant answers the same common questions many times a day through a paid model service. Each repeat call adds waiting time and a charge. What standard engineering idea addresses exactly this waste?",
      "options": [
        "Store each answer and reuse it when the same question appears again",
        "Send every repeat question to the most expensive option that is available",
        "Delete all previous answers so every question starts from nothing",
        "Make every request slower so that users ask fewer questions"
      ],
      "correct": 0,
      "explanation": "Reusing prior results saves time and money on repeated work. This lesson builds on that idea by adding similarity-aware reuse and spending controls for model calls."
    },
    {
      "stage": "check",
      "question": "Why does `ExactCache.get(model, messages, temperature=0.7)` return `None` even after the same request was stored?",
      "options": [
        "The hash excludes temperature, so every nonzero-temperature request collides with a zero-temperature entry",
        "The cache intentionally bypasses exact reuse when temperature is above zero, incrementing misses instead",
        "The cache stores only responses from `gpt-4o-mini`, not responses from the selected model",
        "TTL is measured in request count, and the first lookup always expires immediately"
      ],
      "correct": 1,
      "explanation": "Both `get` and `put` treat nonzero temperature as non-cacheable. This avoids reusing a stochastic response when the caller asked the model to sample."
    },
    {
      "stage": "check",
      "question": "A `SemanticCache` contains the query `What is the return policy?`. What happens on an exact repeat before its TTL expires?",
      "options": [
        "It is always a miss because semantic caching requires different wording",
        "It evicts the old entry first so each query has at most one miss and one hit",
        "An exact repeat has similarity 1.0 and can hit",
        "It calls the model again and stores the second response under a new vector"
      ],
      "correct": 2,
      "explanation": "`simple_embed` normalizes the same word-count vector on both calls, giving a dot product of 1.0. If the entry is fresh and the threshold is no greater than 1.0, `get` records a hit and returns the original response."
    },
    {
      "stage": "check",
      "question": "A new free-tier user has a full token bucket. What does `TokenBucketRateLimiter.check(\"u1\", 1000)` report before any requests are consumed?",
      "options": [
        "`allowed: false`, because free users cannot spend tokens until they authenticate",
        "`allowed: true` with 500 tokens available, because the refill rate is the bucket capacity",
        "A rate-limit error, because the first call is counted before the check",
        "`allowed: true` with the initial 50,000 tokens"
      ],
      "correct": 3,
      "explanation": "A new free bucket starts at capacity 50,000. A 1,000-token request fits and no timestamp has reached the 10-RPM ceiling, so the check permits it; `consume` must be called separately."
    },
    {
      "stage": "post",
      "question": "How does `classify_complexity(\"What time are you open?\")` route this query?",
      "options": [
        "`simple`, because it is short and matches `what time`",
        "`medium`, because all questions are medium until a model sees the answer",
        "`complex`, because the word `open` implies a multi-step operation",
        "`simple` only after a semantic cache miss; cache state changes the classifier"
      ],
      "correct": 0,
      "explanation": "The classifier first checks short queries and `SIMPLE_KEYWORDS`, which includes `what time`. It returns `simple` without consulting cache state or a model."
    },
    {
      "stage": "post",
      "question": "For `route_model(\"Design a retrieval architecture and explain the trade-off\", tier=\"pro\")`, which model is selected?",
      "options": [
        "`gpt-4.1-nano`, because all pro traffic is routed to the cheapest model",
        "`gpt-4o`, because the query is classified as complex",
        "`claude-haiku-3.5`, because complex questions always use the smallest Claude model",
        "`gpt-4o-mini`, because the word count is greater than five but no simple keyword appears"
      ],
      "correct": 1,
      "explanation": "`design` and `trade-off` are in `COMPLEX_KEYWORDS`, so complexity is `complex`. The routing table maps a complex pro request to `gpt-4o`."
    }
  ]
}
