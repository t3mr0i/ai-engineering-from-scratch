{
  "lesson": "09-function-calling",
  "title": "Function Calling & Tool Use",
  "questions": [
    {
      "stage": "pre",
      "question": "After `register_all_tools()`, what does the `get_weather` definition require?",
      "options": [
        "A required city and optional Celsius/Fahrenheit units",
        "A numeric city ID and a mandatory latitude/longitude object",
        "Only `units`; the city is inferred from the user's message by the dispatcher",
        "A complete weather response object supplied by the model before the function runs"
      ],
      "correct": 0,
      "explanation": "The registered JSON schema lists `city` in `required`. `units` has an enum with the two supported strings but is not required, so `get_weather` defaults to Celsius."
    },
    {
      "stage": "check",
      "question": "What does `calculator({\"expression\": \"2+2\"})` return in the Python implementation?",
      "options": [
        "`{\"error\": true}` because arithmetic expressions must contain spaces",
        "A result record containing `result: 4.0` and the original expression",
        "The string `\"2+2\"`, because the tool never evaluates model arguments",
        "A tool-call envelope with a pending callback rather than a numeric result"
      ],
      "correct": 1,
      "explanation": "The calculator permits digits and arithmetic punctuation, evaluates the expression with builtins removed, converts the value to float, and returns the rounded result plus `expression`."
    },
    {
      "stage": "check",
      "question": "Why does `validate_tool_arguments(\"get_weather\", {\"city\": \"Tokyo\", \"units\": \"kelvin\"})` report an error?",
      "options": [
        "`city` is not allowed when `units` is supplied",
        "The validator rejects all optional properties, even when their types are strings",
        "`units` is outside the `celsius`/`fahrenheit` enum",
        "The weather function requires a numeric temperature before it can validate units"
      ],
      "correct": 2,
      "explanation": "The schema explicitly restricts `units` to two values. The validator checks enum membership after checking the property type, so `kelvin` is rejected."
    },
    {
      "stage": "check",
      "question": "What happens when `execute_tool_call` receives `read_file` with `path=\"../etc/passwd\"`?",
      "options": [
        "It reads the first matching file name from `FILE_SYSTEM`",
        "It normalizes the path to `etc/passwd` and then searches the registry",
        "It calls `run_code` instead, because file traversal is treated as Python code",
        "The file tool returns an error with code `FORBIDDEN` before reading anything"
      ],
      "correct": 3,
      "explanation": "`read_file` rejects absolute paths and any path containing `..`. It returns a structured `FORBIDDEN` error, which `execute_tool_call` wraps with the tool name and execution time."
    },
    {
      "stage": "post",
      "question": "A user message contains both `\"weather\"` and `\"calculate 2+2\"`. Which branch does `simulate_model_decision` take?",
      "options": [
        "The weather branch, because weather/temperature checks run before arithmetic checks",
        "Both branches are always returned as one combined tool call",
        "The calculator branch, because arithmetic expressions have higher priority than weather",
        "No branch, because a message with two intents is rejected as ambiguous"
      ],
      "correct": 0,
      "explanation": "The dispatcher checks weather keywords first and returns immediately with one or more `get_weather` calls. The later calculator branch is not reached for that message."
    },
    {
      "stage": "post",
      "question": "What does `run_function_calling_loop(\"Tell me a joke\")` record?",
      "options": [
        "A calculator call with expression `0`, because every message needs at least one tool",
        "No tool results and zero iterations, because no registered intent matches",
        "A weather call for Tokyo, which is the simulator's default city",
        "A web search for `joke`, because all unknown requests are routed to search"
      ],
      "correct": 1,
      "explanation": "No weather, arithmetic, search, file, or code keyword matches. `simulate_model_decision` returns an empty list, so the loop breaks without appending tool messages or results."
    }
  ]
}
