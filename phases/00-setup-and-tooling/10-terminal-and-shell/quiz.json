{
  "lesson": "10-terminal-and-shell",
  "title": "Terminal & Shell",
  "questions": [
    {
      "stage": "pre",
      "question": "What does the pipe in tail -f train.log | grep loss do?",
      "options": [
        "It runs tail and grep on unrelated files in parallel",
        "It appends the entire log to a new binary file",
        "It pipes stdout into grep",
        "It encrypts the log before displaying it"
      ],
      "correct": 2,
      "explanation": "The pipe connects standard output to standard input. tail follows new log lines and grep filters the stream for the requested text."
    },
    {
      "stage": "check",
      "question": "What is the difference between these redirections: > output.log and >> output.log?",
      "options": [
        "> appends; >> truncates the file before writing",
        "Both send output to the terminal and leave the file unchanged",
        "Both redirect only standard error",
        "> replaces; >> appends"
      ],
      "correct": 3,
      "explanation": "A single greater-than opens the destination for replacement, while the double form opens it for append. Use the latter when preserving earlier training runs matters."
    },
    {
      "stage": "check",
      "question": "Why is tmux useful for a long training run on an SSH GPU box?",
      "options": [
        "Detach and reattach to the same session",
        "It turns a failed training process into a system service automatically",
        "It doubles the GPU VRAM available to Python",
        "It compresses every checkpoint before the program writes it"
      ],
      "correct": 0,
      "explanation": "The tmux server stays alive after the SSH client disconnects. Reattaching shows the same shell and output, and panes can hold training and monitoring commands together."
    },
    {
      "stage": "check",
      "question": "Which command copies a local experiment directory to a remote GPU box while preserving useful file metadata?",
      "options": [
        "grep -r experiment user@gpu-box",
        "rsync -av over SSH",
        "nvidia-smi ./experiment/ user@gpu-box",
        "python -m scp --metadata ./experiment/"
      ],
      "correct": 1,
      "explanation": "rsync's archive mode preserves common metadata and transfers the directory over SSH. It can later send only changed files."
    },
    {
      "stage": "post",
      "question": "You lose your SSH connection halfway through training and want to see whether the job continues. What should you do next time?",
      "options": [
        "Run it in the foreground and close the terminal quickly",
        "Pipe stderr to /dev/null so SIGHUP cannot reach the process",
        "Run it inside tmux",
        "Copy the command into the local browser address bar"
      ],
      "correct": 2,
      "explanation": "A tmux session owns the shell independently of the SSH client. After reconnecting, tmux attach restores the running view instead of relying on a fragile foreground process."
    },
    {
      "stage": "post",
      "question": "Which sequence gives a useful remote training workspace with live GPU monitoring?",
      "options": [
        "Start nvidia-smi on the laptop, then copy its screen into the remote shell",
        "Use scp to execute the training loop and delete the remote log",
        "Run the job with > /dev/null so no process needs monitoring",
        "Use tmux with a monitoring pane"
      ],
      "correct": 3,
      "explanation": "The session keeps the job alive, a second pane exposes device utilization, and reattachment preserves both views. Logging stdout and stderr separately can complement the live pane."
    }
  ]
}
