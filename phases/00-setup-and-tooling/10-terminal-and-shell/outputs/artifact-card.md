# Terminal & Shell — reusable artifact

Use this card to apply the lesson outcome outside the walkthrough. Its primary goal is to use piping, redirects, and `grep` to filter and process training logs from the command line.

## Decision template

- **Situation:** Name the concrete context in which you need to use piping, redirects, and `grep` to filter and process training logs from the command line.
- **Options:** Compare at least two approaches while you create persistent tmux sessions with multiple panes for concurrent training and GPU monitoring.
- **Failure probe:** Add a counterexample that tests whether you can monitor system and GPU resources with `htop`, `nvtop`, and `nvidia-smi`.
- **Acceptance evidence:** Define an observable check that demonstrates you can transfer files between local and remote machines using SSH, `scp`, and `rsync`.

## Reuse checklist

- State assumptions before making the decision.
- Preserve the rejected alternative and the evidence that ruled it out.
- Revisit the decision when an assumption or acceptance threshold changes.

