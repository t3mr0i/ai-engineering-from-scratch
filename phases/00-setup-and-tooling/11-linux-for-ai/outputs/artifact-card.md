# Linux for AI — reusable artifact

Use this card to apply the lesson outcome outside the walkthrough. Its primary goal is to navigate the Linux file system and perform essential file operations from the command line.

## Decision template

- **Situation:** Name the concrete context in which you need to navigate the Linux file system and perform essential file operations from the command line.
- **Options:** Compare at least two approaches while you manage file permissions with `chmod` and `chown` to resolve "Permission denied" errors.
- **Failure probe:** Add a counterexample that tests whether you can install system packages with `apt` and set up a fresh GPU box for AI work.
- **Acceptance evidence:** Define an observable check that demonstrates you can identify macOS-to-Linux differences that commonly trip up developers working on remote machines.

## Reuse checklist

- State assumptions before making the decision.
- Preserve the rejected alternative and the evidence that ruled it out.
- Revisit the decision when an assumption or acceptance threshold changes.

