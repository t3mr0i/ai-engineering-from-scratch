{
  "lesson": "02-git-and-collaboration",
  "title": "Git & Collaboration",
  "questions": [
    {
      "stage": "pre",
      "question": "You changed train.py and want the change on the shared remote. Which order is correct?",
      "options": [
        "git push, git commit, git add",
        "git clone train.py, git push, git commit",
        "Stage, commit, then push",
        "git commit, git add, git push"
      ],
      "correct": 2,
      "explanation": "git add stages the file, git commit records a local snapshot, and git push sends that snapshot to the configured remote."
    },
    {
      "stage": "check",
      "question": "What happens when you run git checkout -b experiment/new-optimizer?",
      "options": [
        "Git downloads a remote branch with that name",
        "Git deletes the current branch and renames it",
        "Git merges the branch into main",
        "Creates and checks out that branch"
      ],
      "correct": 3,
      "explanation": "The -b option asks Git to create experiment/new-optimizer from the current commit and check it out immediately."
    },
    {
      "stage": "check",
      "question": "Why should .pt, .pth, and .safetensors usually be listed in .gitignore?",
      "options": [
        "They are large model checkpoints",
        "Git refuses to track any binary file",
        "They are shell scripts that need execute permission",
        "They contain the commit history for the repository"
      ],
      "correct": 0,
      "explanation": "Checkpoint files can consume hundreds of megabytes or more. Ignoring them keeps source history usable; a project can publish them through dedicated artifact storage instead."
    },
    {
      "stage": "check",
      "question": "Which command shows a compact branch graph with commit decorations?",
      "options": [
        "git status --history --branches",
        "A compact decorated commit graph",
        "git diff --remote --compact",
        "git branch --commit-files"
      ],
      "correct": 1,
      "explanation": "git log accepts these options to show abbreviated commits, the parent graph, and names such as branches and tags."
    },
    {
      "stage": "post",
      "question": "After git add ., you notice that model.pt is staged. What is the safest immediate fix?",
      "options": [
        "git push model.pt and delete it from the remote later",
        "git commit --amend --no-edit without changing the index",
        "Unstage it, ignore it, and recheck status",
        "git branch -D main"
      ],
      "correct": 2,
      "explanation": "Unstaging the checkpoint prevents it from entering the next commit. Adding the ignore rule and checking status prevents the same mistake on the next add."
    },
    {
      "stage": "post",
      "question": "A teammate wants to merge an optimizer experiment without disturbing main. Which sequence best preserves an inspectable history?",
      "options": [
        "Edit directly on main and force-push every intermediate file",
        "Delete the experiment branch before anyone can inspect it",
        "Copy the repository directory and never record which version was used",
        "Use a reviewed experiment branch, then merge it"
      ],
      "correct": 3,
      "explanation": "An isolated branch keeps the experiment reversible. A focused commit and passing tests give collaborators something concrete to review before the merge."
    }
  ]
}
