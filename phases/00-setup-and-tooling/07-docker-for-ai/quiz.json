{
  "lesson": "07-docker-for-ai",
  "title": "Docker for AI",
  "questions": [
    {
      "stage": "pre",
      "question": "Which Docker storage choice keeps model files when an image is rebuilt?",
      "options": [
        "A named or bind-mounted volume outside the disposable container layer",
        "A Dockerfile comment",
        "A temporary container layer only",
        "The image tag name"
      ],
      "correct": 0,
      "explanation": "The repository, dataset, and named model-cache mounts live outside the writable container layer. Rebuilding software does not recreate those stored files."
    },
    {
      "stage": "check",
      "question": "What combination lets the Compose service request the host NVIDIA GPU?",
      "options": [
        "Only a larger Dockerfile and the Python standard library",
        "A CPU-only image with CUDA disabled",
        "A host driver/runtime plus the Compose device reservation",
        "A README bind mount and an exposed TCP port"
      ],
      "correct": 2,
      "explanation": "The Compose file requests `capabilities: [gpu]`, but the host driver and container runtime still have to satisfy that request. A Python package or port does not create device access."
    },
    {
      "stage": "check",
      "question": "What does inspect_container_config() recognize as the persistent model store?",
      "options": [
        "Every file copied into the image",
        "The `model_cache:` named volume in Compose",
        "The container's stdout buffer",
        "The absence of an EXPOSE declaration"
      ],
      "correct": 1,
      "explanation": "The parser checks for `model_cache:` in the Compose file. The volume is mounted at `/models`; the static flag does not prove Docker created or populated it."
    },
    {
      "stage": "check",
      "question": "Why does the checked-in configuration report an empty `exposed_ports` list?",
      "options": [
        "The parser cannot read Dockerfile instructions",
        "The Compose GPU reservation hides ports",
        "The image uses a hidden notebook server",
        "The Dockerfile intentionally has no `EXPOSE` instruction"
      ],
      "correct": 3,
      "explanation": "This minimal fixture runs a self-terminating Python readiness command and does not advertise a service port. The parser therefore returns an empty list."
    },
    {
      "stage": "post",
      "question": "The summary reports `persistent_volume: false` after a temporary Compose edit. Which change restores the lesson contract?",
      "options": [
        "Add the `model_cache:` named volume and mount it at `/models`",
        "Change the CUDA tag to `latest`",
        "Add an unrelated HTTP port",
        "Copy model files into the image layer"
      ],
      "correct": 0,
      "explanation": "The parser's persistence check is tied to the named model-cache volume. A pinned image, port, or copied file does not preserve data across container recreation."
    },
    {
      "stage": "post",
      "question": "Which plan keeps this minimal container reproducible while stating what remains unverified?",
      "options": [
        "Use latest everywhere and claim the GPU works because the YAML parses",
        "Bake credentials into the image and remove all mounts",
        "Pin the image, keep allowlisted packages, retain model/data mounts, and test Docker runtime separately",
        "Start a vector database that is not present in the Compose file"
      ],
      "correct": 2,
      "explanation": "The static audit supports the pinned image, allowlisted layer, GPU request, and mounts. Docker build, driver visibility, and runtime health need their own observed checks."
    }
  ]
}
