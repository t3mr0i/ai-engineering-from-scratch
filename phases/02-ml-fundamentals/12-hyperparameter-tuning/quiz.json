{
  "lesson": "12-hyperparameter-tuning",
  "title": "Hyperparameter Tuning",
  "questions": [
    {
      "stage": "pre",
      "question": "Which value is a hyperparameter rather than a parameter learned inside a linear model?",
      "options": [
        "A fitted regression weight",
        "The intercept after optimization",
        "The learning rate chosen before training",
        "A residual computed from a training row"
      ],
      "correct": 2,
      "explanation": "The learning rate controls the optimization procedure and is selected outside the fitted model parameters."
    },
    {
      "stage": "check",
      "question": "A grid contains 3 learning rates and 2 regularization strengths. How many combinations does a full grid search evaluate?",
      "options": [
        "5",
        "3",
        "9",
        "6"
      ],
      "correct": 3,
      "explanation": "The Cartesian product has 3*2=6 combinations."
    },
    {
      "stage": "check",
      "question": "Why can random search be more efficient than a grid when only one of many hyperparameters matters strongly?",
      "options": [
        "Random draws explore more distinct values along the important dimension",
        "Random search always evaluates fewer models than the requested budget",
        "A grid cannot tune a numeric learning rate",
        "Random search uses the test labels to select each draw"
      ],
      "correct": 0,
      "explanation": "A grid spends many trials repeating similar values in unimportant dimensions; random samples cover the influential dimension more broadly."
    },
    {
      "stage": "check",
      "question": "In Bayesian optimization, what does an acquisition function choose?",
      "options": [
        "The final test label for the current model",
        "The next hyperparameter point by trading off predicted value and uncertainty",
        "The number of classes from the training set",
        "A preprocessing column without evaluating a model"
      ],
      "correct": 1,
      "explanation": "The surrogate predicts promising regions and uncertainty; the acquisition rule balances exploiting high predictions with exploring uncertain points."
    },
    {
      "stage": "post",
      "question": "You tried hundreds of settings on one validation set. What protects the final estimate from tuning overfit?",
      "options": [
        "Choose the configuration with the largest training error",
        "Use the same validation labels as the model's training targets",
        "Keep a test set untouched until the selected configuration is finalized",
        "Report the best validation score as an unbiased test score"
      ],
      "correct": 2,
      "explanation": "Repeatedly selecting on a validation set adapts to its noise. A held-out test set or nested cross-validation gives a cleaner final estimate."
    },
    {
      "stage": "post",
      "question": "What does patience in early stopping control?",
      "options": [
        "How many features are permanently removed before the first epoch",
        "How many test examples are relabeled after each epoch",
        "How many random seeds are averaged in one gradient",
        "How many non-improving validation checks are tolerated before stopping"
      ],
      "correct": 3,
      "explanation": "Patience prevents a temporary plateau from ending training immediately while still stopping once validation performance fails to improve for long enough."
    }
  ]
}
