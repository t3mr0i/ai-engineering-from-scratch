# Recommender evaluation card

Record the interaction signal, exposure assumptions, split timestamp, candidate policy, consumed-item mask, and cold-start fallback. Report Precision@K, Recall@K, and NDCG@K beside coverage, diversity, concentration, safety constraints, and latency. Preserve the popularity baseline and identify which statistics were fit on training history so future interactions cannot leak into the evaluation.
