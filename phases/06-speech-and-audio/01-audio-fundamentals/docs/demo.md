# Guided demo: Audio Fundamentals — Waveforms, Sampling, Fourier Transform

> **Time:** 10–15 minutes · **Question:** What observable evidence shows that you can explain the signal-processing and modeling concepts behind Audio Fundamentals — Waveforms, Sampling, Fourier Transform?

## Before you run

Write one predicted invariant for the baseline. Tie it to this objective: **Explain the signal-processing and modeling concepts behind Audio Fundamentals — Waveforms, Sampling, Fourier Transform.** Do not inspect the output first.

## Run the baseline

From the repository root:

```bash
python3 phases/06-speech-and-audio/01-audio-fundamentals/code/main.py
```

The command must print a bounded result and exit with status 0. Locate the part of the output that provides evidence for **Explain the signal-processing and modeling concepts behind Audio Fundamentals — Waveforms, Sampling, Fourier Transform**. Record the exact input, the relevant output, and the invariant in one sentence.

## Change one variable

Change the smallest input or configuration value that helps you investigate **Implement the lesson's core audio operation from first principles**. Keep every other value fixed. Run the same command again and capture a before/after pair; a screenshot without the values is not sufficient evidence.

## Probe a failure

Choose an edge case or violated precondition related to **Inspect time-, frequency-, or token-domain intermediates produced by the pipeline**. Predict whether the program should reject it, degrade gracefully, or return a different valid result. Run the probe and explain any mismatch between prediction and observation. Restore the source afterward.

## Exit ticket

In three sentences, state (1) the mechanism you observed, (2) the controlled change and its effect, and (3) the acceptance check that demonstrates you can **evaluate quality, latency, and robustness trade-offs for Audio Fundamentals — Waveforms, Sampling, Fourier Transform**. If the evidence is ambiguous, name the next measurement rather than claiming success.

