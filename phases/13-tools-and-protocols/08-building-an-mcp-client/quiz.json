{
  "lesson": "08-building-an-mcp-client",
  "title": "Building an MCP Client — Discovery, Invocation, Session Management",
  "questions": [
    {
      "stage": "pre",
      "question": "An assistant needs tools from a file store, a database, and a ticket system, each owned by a different team. How should the host combine them?",
      "options": [
        "Copy all three implementations into the host's codebase",
        "Pick one system and ignore the other two",
        "Ask the model to choose which system exists",
        "Connect to each server, merge their tool lists into one namespace, and route calls by name"
      ],
      "correct": 3,
      "explanation": "Each system stays behind its own server; the client merges and routes. This lesson builds that client: discovery, namespacing, and session handling."
    },
    {
      "stage": "check",
      "question": "Where does the canonical files/search name route?",
      "options": [
        "The notes server's local search function",
        "files/search -> files.search",
        "The GitHub server's list_issues function",
        "No server; it is a display-only alias"
      ],
      "correct": 1,
      "explanation": "The prefix identifies the files session while preserving its local tool name search for dispatch."
    },
    {
      "stage": "check",
      "question": "What happens to the first unqualified search name under prefix-on-collision?",
      "options": [
        "It is deleted because search is ambiguous",
        "It becomes github/search immediately",
        "The first keeps search; later names are prefixed",
        "All three servers are renamed with numeric suffixes"
      ],
      "correct": 2,
      "explanation": "The merge policy preserves the first registration as search and qualifies subsequent duplicate names such as files/search and github/search."
    },
    {
      "stage": "check",
      "question": "What does a call to notes create report after the notes session is marked dead?",
      "options": [
        "A successful note with a new id",
        "A retry against the files server",
        "A JSON-RPC parse error",
        "isError: session dead: notes"
      ],
      "correct": 3,
      "explanation": "Session.alive is checked before routing, so a dead session returns an explicit session-dead tool error."
    },
    {
      "stage": "post",
      "question": "What does merge(policy=reject) do when a later server exports an existing name?",
      "options": [
        "skip the later duplicate",
        "It sends both calls to the later server",
        "It changes every name to a UUID",
        "It raises before retaining any tools"
      ],
      "correct": 0,
      "explanation": "The reject policy keeps the first canonical registration and omits later tools whose local name collides."
    },
    {
      "stage": "post",
      "question": "What state does initialize_all record for each successful Session?",
      "options": [
        "Only the server name; discovery happens on every call",
        "The server capabilities and alive=True",
        "A permanent OAuth token",
        "A single global tool list with no session identity"
      ],
      "correct": 1,
      "explanation": "Initialization stores each server's advertised capabilities and marks its session alive before discovery and invocation."
    }
  ]
}
