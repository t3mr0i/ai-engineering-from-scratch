{
  "lesson": "21-llm-routing-layer",
  "title": "LLM Routing Layer — LiteLLM, OpenRouter, Portkey",
  "questions": [
    {
      "stage": "pre",
      "question": "Which fallback chain does the smart alias use?",
      "options": [
        "anthropic/claude-haiku, then openai/gpt-4o-mini",
        "The smart alias has no fallback",
        "openai/gpt-4o -> anthropic/claude-sonnet -> google/gemini-pro",
        "google/gemini-pro, then openai/gpt-4o, then anthropic/claude-haiku"
      ],
      "correct": 2,
      "explanation": "ROUTES maps smart to openai/gpt-4o, anthropic/claude-sonnet, then google/gemini-pro, so an outage can advance through those providers in order."
    },
    {
      "stage": "check",
      "question": "What model chain does the fast alias select?",
      "options": [
        "openai/gpt-4o -> anthropic/claude-sonnet",
        "anthropic/claude-sonnet -> google/gemini-pro",
        "anthropic/claude-haiku -> openai/gpt-4o",
        "openai/gpt-4o-mini -> anthropic/claude-haiku"
      ],
      "correct": 3,
      "explanation": "The fast route starts with openai/gpt-4o-mini and falls back to anthropic/claude-haiku."
    },
    {
      "stage": "check",
      "question": "What happens to an SSN in messages before provider_call?",
      "options": [
        "redacts to [REDACTED]; redacted=True",
        "It is sent unchanged to every fallback",
        "It is hashed but still shown in the prompt",
        "It causes routing to fail before any attempt"
      ],
      "correct": 0,
      "explanation": "redact_pii replaces the SSN pattern (and card pattern) before the provider request, and the invocation records that redaction occurred."
    },
    {
      "stage": "check",
      "question": "What result does route return when every provider in an alias chain is in OUTAGE?",
      "options": [
        "The last provider's stale response",
        "all providers failed",
        "A successful empty completion",
        "A 403 scope error"
      ],
      "correct": 1,
      "explanation": "Each provider failure is recorded; if none succeeds, the invocation returns the explicit all-providers-failed error."
    },
    {
      "stage": "post",
      "question": "How is a successful invocation cost computed?",
      "options": [
        "By counting only prompt characters",
        "As a flat fee per route",
        "token rates per million applied to usage",
        "By using the number of fallback attempts as dollars"
      ],
      "correct": 2,
      "explanation": "The pricing table is per million tokens, so route combines prompt and completion usage with the selected model's input/output rates."
    },
    {
      "stage": "post",
      "question": "Why does the route alias help the application?",
      "options": [
        "It makes every provider return identical hidden states",
        "It removes the need to measure latency",
        "It guarantees the first provider is always available",
        "alias owns selection and fallback"
      ],
      "correct": 3,
      "explanation": "Aliases decouple the caller from concrete provider names, allowing routing, outage fallback, and pricing policy to change centrally."
    }
  ]
}
