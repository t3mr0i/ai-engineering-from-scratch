{
  "lesson": "19-a2a-protocol",
  "title": "A2A — Agent-to-Agent Protocol",
  "questions": [
    {
      "stage": "pre",
      "question": "Which learning outcome is central to A2A — Agent-to-Agent Protocol?",
      "options": [
        "Distinguish agent-to-tool (MCP) from agent-to-agent (A2A) use cases",
        "Skip validation and trust the first plausible-looking output",
        "Choose a production framework before defining the core mechanism",
        "Treat one successful example as proof that every edge case works"
      ],
      "correct": 0,
      "explanation": "“Distinguish agent-to-tool (MCP) from agent-to-agent (A2A) use cases” is one of this lesson’s stated outcomes; the other choices bypass its required reasoning or validation."
    },
    {
      "stage": "check",
      "question": "Which task best demonstrates practical understanding of A2A — Agent-to-Agent Protocol?",
      "options": [
        "Skip validation and trust the first plausible-looking output",
        "Publish an Agent Card at `/.well-known/agent.json` with skills and endpoint metadata",
        "Choose a production framework before defining the core mechanism",
        "Treat one successful example as proof that every edge case works"
      ],
      "correct": 1,
      "explanation": "“Publish an Agent Card at `/.well-known/agent.json` with skills and endpoint metadata” is one of this lesson’s stated outcomes; the other choices bypass its required reasoning or validation."
    },
    {
      "stage": "check",
      "question": "What should a learner inspect while working through A2A — Agent-to-Agent Protocol?",
      "options": [
        "Skip validation and trust the first plausible-looking output",
        "Choose a production framework before defining the core mechanism",
        "Walk the Task lifecycle (submitted → working → input-required → completed / failed / canceled / rejected)",
        "Treat one successful example as proof that every edge case works"
      ],
      "correct": 2,
      "explanation": "“Walk the Task lifecycle (submitted → working → input-required → completed / failed / canceled / rejected)” is one of this lesson’s stated outcomes; the other choices bypass its required reasoning or validation."
    },
    {
      "stage": "check",
      "question": "Which evaluation habit best fits A2A — Agent-to-Agent Protocol?",
      "options": [
        "Skip validation and trust the first plausible-looking output",
        "Use Messages with Parts (text, file, data) and Artifacts as outputs",
        "Choose a production framework before defining the core mechanism",
        "Treat one successful example as proof that every edge case works"
      ],
      "correct": 1,
      "explanation": "“Use Messages with Parts (text, file, data) and Artifacts as outputs” is one of this lesson’s stated outcomes; the other choices bypass its required reasoning or validation."
    },
    {
      "stage": "post",
      "question": "Which workflow provides the strongest evidence of mastery for A2A — Agent-to-Agent Protocol?",
      "options": [
        "Skip validation and trust the first plausible-looking output",
        "Choose a production framework before defining the core mechanism",
        "Treat one successful example as proof that every edge case works",
        "Distinguish agent-to-tool (MCP) from agent-to-agent (A2A) use cases"
      ],
      "correct": 3,
      "explanation": "“Distinguish agent-to-tool (MCP) from agent-to-agent (A2A) use cases” is one of this lesson’s stated outcomes; the other choices bypass its required reasoning or validation."
    },
    {
      "stage": "post",
      "question": "What is the best completion criterion for A2A — Agent-to-Agent Protocol?",
      "options": [
        "Publish an Agent Card at `/.well-known/agent.json` with skills and endpoint metadata",
        "Skip validation and trust the first plausible-looking output",
        "Choose a production framework before defining the core mechanism",
        "Treat one successful example as proof that every edge case works"
      ],
      "correct": 0,
      "explanation": "“Publish an Agent Card at `/.well-known/agent.json` with skills and endpoint metadata” is one of this lesson’s stated outcomes; the other choices bypass its required reasoning or validation."
    }
  ]
}
