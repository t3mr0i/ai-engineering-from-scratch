{
  "lesson": "03-parallel-and-streaming-tool-calls",
  "title": "Parallel Tool Calls and Streaming with Tools",
  "questions": [
    {
      "stage": "pre",
      "question": "For the three bundled weather calls, why is the parallel wall time near 800 ms instead of 1800 ms?",
      "options": [
        "The parallel wall time is set by the 800 ms maximum",
        "The calls are cached and no executor runs",
        "The code silently drops the 600 and 800 ms calls",
        "Each call is changed to an 800 ms timeout in sequence"
      ],
      "correct": 0,
      "explanation": "Sequential execution adds the city latencies to about 1800 ms; the thread pool overlaps them and therefore approaches the slowest 800 ms call."
    },
    {
      "stage": "check",
      "question": "How does StreamAccumulator keep interleaved argument chunks separate?",
      "options": [
        "It appends every chunk to one global string",
        "It appends each chunk to the buffer named by its call id",
        "It sorts chunks alphabetically before decoding them",
        "It executes a call as soon as its first chunk arrives"
      ],
      "correct": 1,
      "explanation": "Each call_start creates a CallBuffer keyed by id, and later args_delta events append only to that buffer."
    },
    {
      "stage": "check",
      "question": "What does CallBuffer.try_parse return before call_stop marks the buffer done?",
      "options": [
        "An empty Call with default city",
        "A partially parsed dictionary",
        "None",
        "A decode error for every partial chunk"
      ],
      "correct": 2,
      "explanation": "try_parse waits for done so incomplete JSON is not decoded; once done it calls json.loads on the complete argument string."
    },
    {
      "stage": "check",
      "question": "Which complete argument object is reconstructed for the fake stream's C call?",
      "options": [
        "{city: Bengaluru}",
        "{city: Tokyo}",
        "{city: Zurich, units: celsius}",
        "{\"city\":\"Zurich\"}"
      ],
      "correct": 3,
      "explanation": "The C chunks are {\"city\":\"Zu, rich\"}, yielding the valid JSON object with city Zurich and no units field."
    },
    {
      "stage": "post",
      "question": "Why does replay_and_execute return a result mapping rather than a list in arrival order?",
      "options": [
        "Results are keyed by call id",
        "Lists cannot hold JSON values",
        "The code uses city names as globally unique thread ids",
        "The executor returns no result until all calls have identical latency"
      ],
      "correct": 0,
      "explanation": "The stream may interleave calls and futures may finish out of order; call ids preserve the request-to-result association."
    },
    {
      "stage": "post",
      "question": "When should an application avoid parallelizing otherwise independent tool calls?",
      "options": [
        "When all calls return strings",
        "When order-dependent side effects or shared state are involved",
        "When a call has a JSON object argument",
        "When the tool registry contains more than one function"
      ],
      "correct": 1,
      "explanation": "Parallel execution is safe only when operations are independent; writes or order-sensitive effects may require sequential coordination."
    }
  ]
}
