{
  "lesson": "15-mcp-security-tool-poisoning",
  "title": "MCP Security I — Tool Poisoning, Rug Pulls, Cross-Server Shadowing",
  "questions": [
    {
      "stage": "pre",
      "question": "Which static pattern hits does scan_description find in calculator_add?",
      "options": [
        "Only a long-word nit",
        "Only a duplicate-name warning",
        "SYSTEM and secret-read pattern hits",
        "No finding until a user executes the tool"
      ],
      "correct": 2,
      "explanation": "The scanner matches the lowercased <system> tag and the ~/.ssh/id_rsa read request in calculator_add's description."
    },
    {
      "stage": "check",
      "question": "What does the second scan of the mutated approved tool demonstrate?",
      "options": [
        "A new tool is pinned as clean",
        "The scanner forgets the first hash",
        "The mutation is accepted because the name is unchanged",
        "The changed description is detected as a RUG PULL"
      ],
      "correct": 3,
      "explanation": "An approved tool's stored description hash no longer matches after mutation, so the hash layer blocks it as a rug pull."
    },
    {
      "stage": "check",
      "question": "What is the default result for an unseen clean description when approve_new is false?",
      "options": [
        "A warning that the tool is unknown",
        "An automatic approval and saved hash",
        "A RUG PULL block",
        "A JSON-RPC parse error"
      ],
      "correct": 0,
      "explanation": "Unknown tools are not automatically trusted; without approval the scanner reports an unknown warning rather than pinning the hash."
    },
    {
      "stage": "check",
      "question": "Which digest does hash_description compute?",
      "options": [
        "MD5 of the tool name",
        "SHA-256 of the description text",
        "A random UUID per process",
        "SHA-1 of the schema only"
      ],
      "correct": 1,
      "explanation": "The implementation hashes the description with SHA-256 and stores the resulting hex digest for later comparison."
    },
    {
      "stage": "post",
      "question": "Why are both static patterns and hash pinning useful here?",
      "options": [
        "Hashes replace all need for reviewing new tools",
        "Patterns prove that a tool's backend is safe",
        "patterns catch poison text; hashes catch mutations",
        "They are duplicate checks with identical blind spots"
      ],
      "correct": 2,
      "explanation": "Pattern scanning catches recognizable poison strings, while hash comparison detects a trusted description changing after approval."
    },
    {
      "stage": "post",
      "question": "What happens when an unseen clean tool is scanned with approve_new=True?",
      "options": [
        "It is rejected as a rug pull",
        "It is silently ignored",
        "It receives a hard-coded OAuth scope",
        "It pins the hash and reports newly pinned"
      ],
      "correct": 3,
      "explanation": "The approval path stores the first hash and emits a nit describing that the tool was newly pinned."
    }
  ]
}
