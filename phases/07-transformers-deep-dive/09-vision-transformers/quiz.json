{
  "lesson": "09-vision-transformers",
  "title": "Vision Transformers — Patchify, Embed, Attend",
  "questions": [
    {
      "stage": "pre",
      "question": "Transformers need sequences but images are grids. What conversion bridges that gap?",
      "options": [
        "One token per whole image only",
        "Pixels sorted by brightness",
        "File names as tokens",
        "Non-overlapping flattened patches in grid order"
      ],
      "correct": 3,
      "explanation": "Grids become sequences by patching. This lesson cuts images into transformer tokens."
    },
    {
      "stage": "check",
      "question": "What shape does linear_project produce for those 16 flattened patches with d_model=48?",
      "options": [
        "48×108",
        "16×48",
        "16×108",
        "4×48"
      ],
      "correct": 1,
      "explanation": "The projection maps each 108-value patch to one 48-wide vector while preserving the number of patches."
    },
    {
      "stage": "check",
      "question": "What happens to the sequence length in cls_and_pos for a 4×4 patch grid?",
      "options": [
        "It becomes 48 because position vectors are concatenated to every patch",
        "It remains 16 because [CLS] replaces the first patch",
        "It becomes 17 because one learnable [CLS] vector is prepended to 16 patch tokens",
        "It becomes 20 because four row tokens are added"
      ],
      "correct": 2,
      "explanation": "cls_and_pos starts its output with one CLS vector and then appends every patch after adding its 2D positional encoding."
    },
    {
      "stage": "check",
      "question": "Why does pos_2d require d_model to be divisible by 4?",
      "options": [
        "It divides the image into four equal patch grids",
        "It needs four separate class tokens for the image corners",
        "It stores four RGB values at every spatial position",
        "It splits the width into row and column halves, each needing even sin/cos pairs"
      ],
      "correct": 3,
      "explanation": "Half the dimensions encode rows and half columns; each half is filled with even/odd sinusoidal pairs."
    },
    {
      "stage": "post",
      "question": "Which component supplies the vision-specific tokenization before a standard Transformer encoder?",
      "options": [
        "patchify plus linear_project, followed by [CLS] and 2D position information",
        "The decoder cross-attention and a text tokenizer",
        "A recurrent scan over the image's RGB channels",
        "The classifier head before any patch is formed"
      ],
      "correct": 0,
      "explanation": "The demo makes image patches into token vectors and adds position/CLS information; the later transformer block can reuse an encoder architecture."
    },
    {
      "stage": "post",
      "question": "In param_count_vit, what is counted by `head = d_model * num_classes`?",
      "options": [
        "The Q, K, and V projections for every attention head",
        "The final linear classifier that maps the representation to class logits",
        "The number of spatial patches in the input image",
        "The learned [CLS] vector plus all position embeddings"
      ],
      "correct": 1,
      "explanation": "The classifier head has one output weight per model dimension and class; position and CLS terms are counted separately."
    }
  ]
}
