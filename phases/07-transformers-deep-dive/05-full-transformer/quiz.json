{
  "lesson": "05-full-transformer",
  "title": "The Full Transformer — Encoder + Decoder",
  "questions": [
    {
      "stage": "pre",
      "question": "In the demo, what are the source and target shapes before the encoder and decoder run?",
      "options": [
        "Source is 6×8 and target is 5×8",
        "Source is 8×6 and target is 8×5",
        "Both are 6×6 because attention changes the model width",
        "Source is 5×8 and target is 6×8"
      ],
      "correct": 0,
      "explanation": "main sets src_len=6, tgt_len=5, and d=8; attention changes token interactions but preserves each sequence's model width."
    },
    {
      "stage": "check",
      "question": "What sequence of sublayers does decoder_block apply after its first RMSNorm?",
      "options": [
        "Bidirectional self-attention, pooling, then a classifier with no residuals",
        "Causal self-attention, encoder cross-attention, then an FFN, each with a residual",
        "Only cross-attention to enc_out, followed by a second tokenizer",
        "An FFN, then causal self-attention, while ignoring enc_out"
      ],
      "correct": 1,
      "explanation": "decoder_block first adds masked self-attention, then attends to enc_out, and finally adds the selected FFN output."
    },
    {
      "stage": "check",
      "question": "How does rms_norm differ from layer_norm in this implementation?",
      "options": [
        "RMSNorm normalizes across sequence positions, while LayerNorm uses one token",
        "RMSNorm subtracts the mean twice and never computes a square",
        "RMSNorm divides by root mean square without subtracting the row mean",
        "They are identical except for the function name"
      ],
      "correct": 2,
      "explanation": "rms_norm computes sqrt(mean(v²)+eps) and divides; layer_norm also computes and subtracts the row mean."
    },
    {
      "stage": "check",
      "question": "What operation defines ffn_swiglu(X, W1, W2, W3)?",
      "options": [
        "A residual addition of W1, W2, and W3 without matrix products",
        "W1 applied after a softmax over tokens, followed by W2+W3",
        "A ReLU on XW2 with W1 and W3 used only for scaling the loss",
        "W2 applied to silu(XW1) elementwise multiplied by XW3"
      ],
      "correct": 3,
      "explanation": "The code forms h1=XW1 and h3=XW3, gates silu(h1) by h3, then projects the result with W2."
    },
    {
      "stage": "post",
      "question": "What does causal=True change inside scaled_dot_product_attention?",
      "options": [
        "It masks score entries where j>i before softmax",
        "It removes the sqrt(dk) scale from every score",
        "It reverses K and V so the decoder reads the encoder backward",
        "It replaces softmax with an argmax over the future"
      ],
      "correct": 0,
      "explanation": "The mask marks future columns j>i as -inf, so their post-softmax weights become zero."
    },
    {
      "stage": "post",
      "question": "Why does decoder_block pass enc_out as kv_source for its cross-attention?",
      "options": [
        "It lets the decoder reuse the source vocabulary without computing attention",
        "Queries come from the decoder state while keys and values come from the encoded source sequence",
        "It makes the decoder causal mask apply to source positions instead of target positions",
        "It replaces the target residual stream with a copy of enc_out"
      ],
      "correct": 1,
      "explanation": "multi_head_attention always forms Q from X but uses kv_source for K and V, which is the encoder-decoder attention pattern."
    }
  ]
}
