{
  "lesson": "14-open-models-architecture-walkthroughs",
  "title": "Open Models: Architecture Walkthroughs",
  "questions": [
        {
      "stage": "pre",
      "question": "You know how a small example model is organized, and you now face a published model with a settings file listing sizes, layer counts, and head counts. What can you do with that file before running anything?",
      "options": ["Estimate the memory and compute the model will need and compare it against the available hardware.", "Delete the settings file because only the weights matter.", "Assume every published model has identical cost regardless of its settings.", "Rename the settings file to make the model run faster."],
      "correct": 0,
      "explanation": "Settings such as sizes and counts predict resource needs. This lesson builds the habit of reading those files field by field for several well-known open models."
    },
    {
      "stage": "check",
      "question": "In the calculator's MoE report, which experts contribute to `active_mlp` for a top-2 configuration with one shared expert?",
      "options": ["All experts, including every inactive route.", "Only the router matrix; no expert MLP.", "One routed expert and two shared experts.", "Two routed experts and the always-on shared expert."],
      "correct": 3,
      "explanation": "The active estimate uses `top_k * expert_params + shared_experts * expert_params`, representing the selected routed experts plus the shared expert."
    },
    {
      "stage": "check",
      "question": "Which formula does `mlp_params` use for a SwiGLU block with hidden size `h` and intermediate size `ff`?",
      "options": ["`3 * h * ff`.", "`2 * h + ff`.", "`4 * h * h` regardless of `ff`.", "`h * ff / 3`."],
      "correct": 0,
      "explanation": "SwiGLU has gate, up, and down projections, so the calculator counts three h-by-ff-sized matrices."
    },
    {
      "stage": "check",
      "question": "Why does the `analyze` report use a latent rank for MLA's KV-cache estimate?",
      "options": ["MLA stores only the vocabulary logits.", "MLA caches a compressed latent representation rather than one full KV vector per KV head.", "MLA has no attention keys or values.", "The latent rank is the number of MoE experts."],
      "correct": 1,
      "explanation": "The code's MLA branch computes cache bytes from `kv_lora_rank`, which is the compressed K/V latent retained across decoding positions."
    },
    {
      "stage": "post",
      "question": "Which routing configuration belongs to the `mixtral8x7b` entry?",
      "options": ["Dense MLP with no experts.", "256 experts with top-8 routing and one shared expert.", "8 experts with top-2 routing and no shared expert.", "One expert selected from 256 for every token."],
      "correct": 2,
      "explanation": "The configuration table sets `experts=8` and `top_k=2` for Mixtral, with MoE enabled; the shared-expert setting is not the DeepSeek-V3 one."
    },
    {
      "stage": "post",
      "question": "For a standard attention layer, which terms does `attention_params_per_layer` add?",
      "options": ["Only the output projection and layer norm.", "The embedding matrix and every expert MLP.", "One Q matrix and one shared scalar.", "Q, K, V, and output projections, with K/V sized by the number of KV heads."],
      "correct": 3,
      "explanation": "The standard branch counts `h*h` for Q, `2*h*(kv_heads*head_dim)` for K and V, and `h*h` for the output projection."
    }
  ]
}
