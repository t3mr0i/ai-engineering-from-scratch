{
  "lesson": "34-gradient-checkpointing",
  "title": "Gradient Checkpointing and Activation Recomputation",
  "questions": [
        {
      "stage": "pre",
      "question": "Training a deep network runs out of memory because every intermediate result is saved for later, but recomputing a result is cheaper than storing all of them. What trade-off would you try?",
      "options": ["Save only a few intermediate results and recompute the rest from the nearest saved one when they are needed.", "Save every intermediate result twice to use even more memory.", "Delete the model weights to free memory.", "Stop training after the first layer and ship that."],
      "correct": 0,
      "explanation": "Keeping a few anchors and rebuilding between them trades extra computation for much lower memory. This lesson builds the method that chooses those anchors and measures the cost."
    },
    {
      "stage": "check",
      "question": "What does `model_backward_checkpointed` do before backpropagating through each saved segment?",
      "options": ["It discards the segment's parameters.", "It replaces the segment with a random layer.", "It uses the saved output as if every intermediate were present.", "It runs `model_forward` on that segment to rebuild its activations."],
      "correct": 3,
      "explanation": "For each segment in reverse order, the function recomputes forward activations from the saved segment input and then calls ordinary backward."
    },
    {
      "stage": "check",
      "question": "What does `checkpoint_cost(n_layers, segment_size=1)` report for recompute FLOPs when `selective=False`?",
      "options": ["0, because each one-layer segment already has its needed input.", "One full extra forward per layer.", "Twice the backward cost.", "The number of saved parameters."],
      "correct": 0,
      "explanation": "The recompute factor is `(segment_size - 1) / segment_size`, which is zero for segment size one."
    },
    {
      "stage": "check",
      "question": "What heuristic does `optimal_segment(n_layers)` use?",
      "options": ["The number of layers divided by two.", "The rounded square root of the layer count, with a minimum of one.", "Always the full model depth.", "The vocabulary size modulo the layer count."],
      "correct": 1,
      "explanation": "The function returns `max(1, int(round(np.sqrt(n_layers))))`, a simple square-root segment heuristic."
    },
    {
      "stage": "post",
      "question": "When does `should_recompute('attention', activation_bytes_mb, ...)` immediately recommend recomputation?",
      "options": ["When the attention activation is below 1 MB.", "Only when the layer has no parameters.", "When attention activations exceed 100 MB.", "When the sequence length is zero."],
      "correct": 2,
      "explanation": "The first branch checks for an attention layer with `activation_bytes_mb > 100` and returns True."
    },
    {
      "stage": "post",
      "question": "What should `verify_equivalence` show when full and checkpointed backward use the same parameters?",
      "options": ["Different outputs because recomputation changes the function.", "A missing gradient for every checkpoint boundary.", "A random output match with no gradient comparison.", "`output_match=True` and a very small maximum gradient difference."],
      "correct": 3,
      "explanation": "Recomputation should reproduce the same forward values and derivatives; the helper checks all gradient arrays with `allclose` and reports their maximum difference."
    }
  ]
}
