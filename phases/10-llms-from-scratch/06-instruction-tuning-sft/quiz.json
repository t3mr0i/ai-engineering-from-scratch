{
  "lesson": "06-instruction-tuning-sft",
  "title": "Instruction Tuning (SFT)",
  "questions": [
    {
      "stage": "pre",
      "question": "Which tokens contribute a nonzero weight in `create_loss_mask` for an instruction pair?",
      "options": ["All prompt and response tokens equally.", "Only `INST_START` and `INST_END`.", "Response tokens after `RESP_START`.", "Padding tokens before the instruction."],
      "correct": 2,
      "explanation": "The mask stays zero through the prompt and separator, then switches to one for tokens in the response."
    },
    {
      "stage": "check",
      "question": "Why does `sft_train` use `mask[1:]` alongside `tokens[1:]`?",
      "options": ["It removes every response token from the loss.", "It converts masks to attention scores.", "It ensures every sequence has exactly `seq_len` targets.", "It aligns the shifted response labels."],
      "correct": 3,
      "explanation": "Inputs are `tokens[:-1]` and targets are `tokens[1:]`; shifting the mask by the same amount labels the target positions correctly."
    },
    {
      "stage": "check",
      "question": "Which IDs does this lesson reserve for `INST_START`, `INST_END`, and `RESP_START`?",
      "options": ["IDs 253, 254, 255.", "0, 1, and 2, respectively.", "256, 257, and 258, respectively.", "255, 254, and 253, respectively."],
      "correct": 0,
      "explanation": "The `SPECIAL_TOKENS` mapping explicitly assigns 253 to the instruction start, 254 to the instruction end, and 255 to the response start."
    },
    {
      "stage": "check",
      "question": "What does `masked_cross_entropy_loss` return when the response mask contains no ones?",
      "options": ["The unmasked loss over the prompt.", "`0.0`.", "Positive infinity.", "The number of prompt tokens."],
      "correct": 1,
      "explanation": "The function checks the masked token count and returns `0.0` when there are no response targets to average."
    },
    {
      "stage": "post",
      "question": "If a formatted instruction sequence exceeds `seq_len`, which part is retained by `sft_train`?",
      "options": ["Only the response, re-tokenized from scratch.", "The last `seq_len` tokens.", "The first `seq_len` tokens.", "The entire sequence, because `seq_len` is advisory."],
      "correct": 2,
      "explanation": "The training loop slices the complete formatted token list with `tokens[:seq_len]` before constructing shifted inputs, targets, and masks."
    },
    {
      "stage": "post",
      "question": "With the same logits, what is the usual effect of lowering the positive `temperature` passed to `generate_response`?",
      "options": ["It makes every token equally likely, regardless of its logit.", "It reverses the token ranking.", "It removes the response mask.", "Lower temperature sharpens sampling."],
      "correct": 3,
      "explanation": "The generator divides logits by temperature before softmax, so a value below one increases their separation and reduces sampling entropy."
    }
  ]
}
