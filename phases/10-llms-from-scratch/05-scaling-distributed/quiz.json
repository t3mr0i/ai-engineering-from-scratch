{
  "lesson": "05-scaling-distributed",
  "title": "Scaling: Distributed Training, FSDP, DeepSpeed",
  "questions": [
        {
      "stage": "pre",
      "question": "You can train a small model on one graphics card, but a larger model runs out of memory on that card. What is the most reasonable way to think about using several cards?",
      "options": ["Different cards can each handle part of the data or part of the model so the whole job fits together.", "Adding more cards automatically makes every program run faster with no code changes.", "Each card must run a completely unrelated training job with no communication.", "More cards remove the need to store the model weights anywhere."],
      "correct": 0,
      "explanation": "Splitting data or model pieces across cards is the starting intuition. This lesson builds the standard splitting strategies and the memory math on top of it."
    },
    {
      "stage": "check",
      "question": "What does `simulate_tensor_parallelism` do with a weight matrix whose output dimension is split across four GPUs?",
      "options": ["It averages four complete matrix products.", "It concatenates column-shard products.", "It shards rows and sums the input features.", "It sends only the bias to GPU 4."],
      "correct": 1,
      "explanation": "Tensor parallelism in this simulator splits the weight columns, computes each partial output, and concatenates them back in output-feature order."
    },
    {
      "stage": "check",
      "question": "Under `memory_calculator(..., sharding='fsdp', num_gpus=4)`, which memory components are divided by four?",
      "options": ["Activations only.", "Weights and activations, but not optimizer state.", "Weights, optimizer state, and gradients.", "Only gradients, because FSDP never shards parameters."],
      "correct": 2,
      "explanation": "The simulator's `fsdp` branch divides the weight, optimizer, and gradient totals by `num_gpus`; activation accounting stays separate."
    },
    {
      "stage": "check",
      "question": "For `P=8` and `M=16`, what expression does `bubble_dualpipe` evaluate?",
      "options": ["`2*(P-1) / (2*M + 2*(P-1))`.", "`M / (P-1)`.", "`(P-1) / (2*M + P-1)`.", "`((P-1)//2) / (3*M + P-1)`."],
      "correct": 3,
      "explanation": "The simulator models DualPipe with total `3*M + (P-1)` slots and a half-depth integer bubble `(P-1)//2`, which is 3/55 here."
    },
    {
      "stage": "post",
      "question": "Which storage pattern does `mixed_precision_comparison` use for its FP16 case?",
      "options": ["FP16 weights and gradients, with FP32 master/Adam state.", "Only FP32 weights; gradients are discarded during optimizer updates.", "FP8 weights with no optimizer state.", "FP16 weights and an integer optimizer state."],
      "correct": 0,
      "explanation": "The FP16 accounting keeps 2-byte weights and gradients, a 4-byte master weight, and 8 bytes for Adam's two FP32 moment buffers."
    },
    {
      "stage": "post",
      "question": "According to `training_cost_estimator`, what leading FLOP estimate is used for dense training?",
      "options": ["`params + tokens`.", "`6 * params * tokens`.", "`2 * layers * hidden_dim`.", "`tokens / acceptance_rate`."],
      "correct": 1,
      "explanation": "The estimator uses the common forward-plus-backward approximation `6 * number_of_parameters * number_of_training_tokens` before applying throughput and utilization."
    }
  ]
}
