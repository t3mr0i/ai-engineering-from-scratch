{
  "lesson": "18-multi-token-prediction",
  "title": "Multi-Token Prediction (MTP)",
  "questions": [
        {
      "stage": "pre",
      "question": "A model trained to predict only the very next word sometimes plans poorly several words ahead. What training change would encourage better planning?",
      "options": ["Ask the model during training to also guess the words two and three steps ahead, not just the immediate next word.", "Hide all future words and forbid the model from ever thinking about them.", "Train the model to repeat the same word forever.", "Delete the training text so there is nothing to predict."],
      "correct": 0,
      "explanation": "Predicting further ahead rewards representations that plan beyond one step. This lesson builds the extra prediction modules and joint training objective that implement that idea."
    },
    {
      "stage": "check",
      "question": "What does the sequential `mtp_forward` step combine before applying `M_k`?",
      "options": ["Two independent output logits.", "The current vocabulary histogram and a loss scalar.", "Only the previous hidden state, with no token embedding.", "RMS-normalized previous hidden state plus RMS-normalized embedding of the next token."],
      "correct": 3,
      "explanation": "The toy folds the concatenation from the paper into an addition of normalized `prev_hidden` and `next_embed`, then projects it."
    },
    {
      "stage": "check",
      "question": "How does `shared_head_logits` produce one vocabulary logit?",
      "options": ["It takes a dot product between the hidden vector and that vocabulary row of the shared embedding E.", "It samples an integer uniformly from the vocabulary.", "It applies a separate random matrix for every depth.", "It returns the hidden vector's RMS norm for every token."],
      "correct": 0,
      "explanation": "The tied head computes `sum(E[v][i] * hidden[i])` for every vocabulary row v, reusing E rather than adding a new output matrix."
    },
    {
      "stage": "check",
      "question": "According to `count_parameters`, which terms make up one toy MTP module's extra count?",
      "options": ["Only the shared embedding and output head.", "`hidden²` for `M_k` plus one attention block and one MLP.", "The entire main model multiplied by D.", "Only the vocabulary size."],
      "correct": 1,
      "explanation": "The code counts `hidden * hidden` for the projection, then adds the attention and MLP terms for each MTP block; embeddings and the head are shared."
    },
    {
      "stage": "post",
      "question": "With a sequence of 12 backbone states and `D=2`, how many starting positions does `mtp_loss` evaluate?",
      "options": ["2", "12", "10", "24"],
      "correct": 2,
      "explanation": "The loop runs `range(len(backbone_hidden) - D)`, so it evaluates 12 - 2 = 10 positions that have two future targets."
    },
    {
      "stage": "post",
      "question": "If every vocabulary logit is equal and the vocabulary has 32 entries, what is the cross-entropy for one target in this toy?",
      "options": ["0", "1", "32", "`ln(32)`"],
      "correct": 3,
      "explanation": "Equal logits give probability 1/32 to the target, so `-log(1/32) = log(32)`, the uniform-guess reference printed by the demo."
    }
  ]
}
