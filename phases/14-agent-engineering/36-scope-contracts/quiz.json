{
  "lesson": "36-scope-contracts",
  "title": "Scope Contracts and Task Boundaries",
  "questions": [
    {
      "stage": "pre",
      "question": "An agent asked to fix a login bug also rewrites the email helper, the database driver, and the release script. What prevents that?",
      "options": [
        "Asking the agent to be more careful",
        "A longer chat history with more context",
        "Trusting the agent's narration of each step",
        "A written scope contract checked against the final diff"
      ],
      "correct": 3,
      "explanation": "Good-faith narration still creeps. This lesson puts the promise on disk and checks the diff against it."
    },
    {
      "stage": "check",
      "question": "What network egress survives the demo's contract merge?",
      "options": [
        "Both `api.openai.com` and `api.anthropic.com`.",
        "No network hosts, because any child list denies all traffic.",
        "Only `evil.example` from the creep run.",
        "The intersection, `api.anthropic.com`."
      ],
      "correct": 3,
      "explanation": "The project allowlist contains two hosts and the child allowlist contains only `api.anthropic.com`; `_merge_egress` intersects them."
    },
    {
      "stage": "check",
      "question": "What does `scope_check` report for the clean run?",
      "options": [
        "`passed=True` and `over_budget=False` with no findings.",
        "`passed=False` because every task must touch a documentation file.",
        "`passed=False` because the task has a 30-minute budget.",
        "`passed=True` but with a forbidden-write block for `app.py`."
      ],
      "correct": 0,
      "explanation": "The clean run touches only effective allowed files, runs the acceptance command, stays within 30 minutes, and uses the allowlisted host, so no finding is emitted."
    },
    {
      "stage": "check",
      "question": "How is `README.md` classified in the creep run?",
      "options": [
        "A block-severity forbidden write.",
        "An informational soft off-scope documentation write.",
        "An in-scope write because README is in every allowed set.",
        "It is ignored because only source files are examined."
      ],
      "correct": 1,
      "explanation": "`README.md` matches `docs_paths_soft` and not the hard allowed/forbidden patterns, so it appears in `soft_off_scope_writes` with an `info` finding."
    },
    {
      "stage": "post",
      "question": "Which creep-run path creates a `scope.forbidden` block?",
      "options": [
        "`README.md`",
        "`app.py`",
        "`scripts/release.sh`",
        "`test_app.py`"
      ],
      "correct": 2,
      "explanation": "The project contract forbids `scripts/release.sh`; the task contract additionally forbids `migrations/**`, so both appear as forbidden writes in the creep report."
    },
    {
      "stage": "post",
      "question": "What is `creep_report.over_budget` in the demo?",
      "options": [
        "`True`, because every informational finding consumes the violation budget.",
        "`True`, because the elapsed time is over 30 minutes.",
        "`True`, because the network host is not allowlisted.",
        "`False`; the report has no warn-severity findings, even though block findings make `passed()` false."
      ],
      "correct": 3,
      "explanation": "`over_budget` counts only findings with severity `warn`; the creep report has blocks and an info finding, so its warn count is zero while `passed()` is still false."
    }
  ]
}
