{
  "lesson": "40-multi-session-handoff",
  "title": "Multi-Session Handoff",
  "questions": [
    {
      "stage": "pre",
      "question": "Which field is the load-bearing one in a handoff packet?",
      "options": [
        "summary",
        "next_action (without it, the document is a status report, not a handoff)",
        "commands_run",
        "verdict_pointer"
      ],
      "correct": 1,
      "explanation": "A handoff with everything except next_action is a status report; the next concrete step is what makes the next session productive."
    },
    {
      "stage": "pre",
      "question": "Why are handoffs generated, not written?",
      "options": [
        "Generators are faster",
        "Hand-written handoffs get skipped on a hard day; the generator reads workbench artifacts and emits the packet, so the agent just leaves the workbench in a state the generator can summarize",
        "Generators encrypt better",
        "Apache 2.0 requires it"
      ],
      "correct": 1,
      "explanation": "Automation closes the gap between intention and consistency."
    },
    {
      "stage": "check",
      "question": "Which two forms does the packet ship in?",
      "options": [
        "PDF and PNG",
        "handoff.md (human-readable) and handoff.json (machine-readable, both from the same source artifacts; JSON wins on divergence)",
        "Email and Slack",
        "YAML and TOML"
      ],
      "correct": 1,
      "explanation": "Markdown for humans, JSON for the next agent; both come from the same generator."
    },
    {
      "stage": "check",
      "question": "What does the open_risks field in a handoff packet answer, per the lesson?",
      "options": [
        "What model was used to generate the handoff packet itself",
        "What could bite next session, plus a severity for each",
        "How long the previous session ran before handing off",
        "Which reviewer signed off on the previous session's work"
      ],
      "correct": 1,
      "explanation": "The lesson's seven-field table lists open_risks as answering 'what could bite next session, with severity'."
    },
    {
      "stage": "check",
      "question": "What does the lesson say the cleanup phase emits before a handoff can be written?",
      "options": [
        "A signed approval token issued by the verification gate",
        "A full diff of every touched file, attached to handoff.json",
        "A rollback script the next session can run automatically",
        "An empty clean_state.json list required before a packet"
      ],
      "correct": 3,
      "explanation": "The lesson says the cleanup phase emits a clean_state.json of blocking issues, and an empty list is the precondition the handoff generator asserts before it writes a packet."
    },
    {
      "stage": "post",
      "question": "What does the lesson recommend trimming the feedback log to in the packet?",
      "options": [
        "First K entries only",
        "Last K entries plus every entry with a non-zero exit, so the packet stays small while the failure history survives",
        "Random N entries",
        "Only the most recent entry"
      ],
      "correct": 1,
      "explanation": "Asymmetric trim: failures must survive; trivia at the tail is cheap to keep."
    },
    {
      "stage": "post",
      "question": "Per the lesson's clean-state table, why does a stray branch block a handoff?",
      "options": [
        "Stray branches make handoff.json fail its JSON Schema validation",
        "Git refuses to generate a diff across more than one branch",
        "The reviewer agent cannot read state from a non-default branch",
        "The next session's first commit would land on the wrong branch"
      ],
      "correct": 3,
      "explanation": "The lesson's table says a wrong branch means the next session's first commit lands in the wrong place, so the check requires the expected branch with no detached HEAD or orphan branches."
    }
  ]
}
