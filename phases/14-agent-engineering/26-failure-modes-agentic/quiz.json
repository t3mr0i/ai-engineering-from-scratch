{
  "lesson": "26-failure-modes-agentic",
  "title": "Failure Modes: Why Agents Break",
  "questions": [
    {
      "stage": "pre",
      "question": "Which label does the detector assign when a trace calls the unknown tool `magic_scanner`?",
      "options": [
        "`tool_misuse`",
        "`scope_creep`",
        "`hallucinated_action`",
        "`context_loss`"
      ],
      "correct": 2,
      "explanation": "`magic_scanner` is not in `KNOWN_TOOLS`, so the first detector flags the action as a hallucinated tool call before later checks run."
    },
    {
      "stage": "check",
      "question": "Why is the `write_file` action in task `t002` tagged `scope_creep`?",
      "options": [
        "The tool name is misspelled and cannot be parsed.",
        "The trace contains an error followed by two downstream calls.",
        "The action omits the required `path` argument.",
        "The request asks to find configuration, without explicitly authorizing a write."
      ],
      "correct": 3,
      "explanation": "The scope detector treats `write_file` as mutating and checks for explicit write/create/save/update/edit language. `t002` has none, so the mutation exceeds the request scope."
    },
    {
      "stage": "check",
      "question": "What makes task `t004` qualify for `cascading_errors`?",
      "options": [
        "An earlier tool error is followed by at least two downstream tool calls.",
        "It calls an unlisted tool before any other action.",
        "It modifies a file without an explicit write request.",
        "Its final claim says success while the target state is unchanged."
      ],
      "correct": 0,
      "explanation": "The cascade detector requires both conditions: an error in the trace and two or more later tool calls that continue from that failed state; `t004` has both."
    },
    {
      "stage": "check",
      "question": "What does the context-loss detector look for in `do not modify src/`?",
      "options": [
        "It extracts `src/` and checks whether the directory exists.",
        "It extracts `modify` and searches serialized tool arguments for that token.",
        "It ignores negated instructions because they are not tool calls.",
        "It extracts `do` and requires every tool argument to contain it."
      ],
      "correct": 1,
      "explanation": "The detector takes the first token after `do not`—`modify`—and checks for that forbidden token in the serialized tool arguments to identify a violated constraint."
    },
    {
      "stage": "post",
      "question": "Why is task `t006` tagged `tool_misuse`?",
      "options": [
        "It calls `read_file`, which is never an allowed tool.",
        "It writes to a path even though the request is read-only.",
        "Its `read_file` call supplies `file` instead of the required `path` argument.",
        "It calls two tools after an earlier error."
      ],
      "correct": 2,
      "explanation": "`read_file` is known, but its schema requires `path`; the fixture passes `file`, so the required-argument detector reports tool misuse."
    },
    {
      "stage": "post",
      "question": "What triggers `success_hallucination` in task `t007`?",
      "options": [
        "A known search tool returns an empty result.",
        "The request contains a negated instruction about a directory.",
        "A tool call has an unknown name and no final response.",
        "The trace claims a write succeeded, but the requested target state did not change."
      ],
      "correct": 3,
      "explanation": "The detector combines write intent, a final success claim, and `target_state_changed=False`; that mismatch is the lesson's success-hallucination case."
    }
  ]
}
