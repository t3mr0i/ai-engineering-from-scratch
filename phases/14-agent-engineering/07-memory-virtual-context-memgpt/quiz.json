{
  "lesson": "07-memory-virtual-context-memgpt",
  "title": "Memory: Virtual Context and MemGPT",
  "questions": [
    {
      "stage": "pre",
      "question": "With `MainContext(max_messages=3)`, what happens when a fourth message is appended?",
      "options": [
        "The oldest message moves to `evicted`, leaving three current messages.",
        "The newest message is discarded and the old three remain.",
        "All messages are copied into `core` and the list is cleared.",
        "The append raises `MemoryError` before changing the context."
      ],
      "correct": 0,
      "explanation": "`append` adds the message and pops index 0 while the list exceeds `max_messages`, recording the removed message in `evicted`."
    },
    {
      "stage": "check",
      "question": "What is the first id returned by `ArchivalStore.insert(\"alpha\")`?",
      "options": [
        "`record-1`",
        "`a001`",
        "`s0-001`",
        "`a000`"
      ],
      "correct": 1,
      "explanation": "`_counter` starts at zero, increments before formatting, and uses `f\"a{self._counter:03d}\"`, yielding `a001`."
    },
    {
      "stage": "check",
      "question": "Which record is returned first by `archival_memory_search(\"tool chains drift\", top_k=2)` in the demo?",
      "options": [
        "The sleep-time compute record `a003`",
        "The user profile record `a001`",
        "`a002: long-horizon tool chains drift after 20 steps per BFCL V4`",
        "`no matches`, because search requires exact phrases"
      ],
      "correct": 2,
      "explanation": "Record `a002` shares all three query tokens, giving it the highest token-overlap score."
    },
    {
      "stage": "check",
      "question": "How can `conversation_search(\"retrieval bot\")` find a message that was evicted from the main context?",
      "options": [
        "It replays the LLM and asks it to regenerate the missing turn.",
        "It searches only `main.messages`, so it cannot find evicted text.",
        "It queries the vector store, which automatically indexes every message.",
        "It searches the combined `evicted + messages` list in reverse order."
      ],
      "correct": 3,
      "explanation": "The method explicitly searches `self.main.evicted + self.main.messages`, so evicted conversation text remains recallable."
    },
    {
      "stage": "post",
      "question": "What does `core_memory_replace(\"persona\", \"missing\", \"new\")` return when `persona` has no such substring?",
      "options": [
        "`error: 'missing' not in core[persona]`",
        "`core[persona] replaced`",
        "`core[persona] appended: 3 chars`",
        "It creates a new `persona` section containing `new`."
      ],
      "correct": 0,
      "explanation": "The method checks membership first and returns an error without changing the core section when `old` is absent."
    },
    {
      "stage": "post",
      "question": "What observation does `run_scripted_agent` record for an unknown tool name?",
      "options": [
        "It skips the call without adding an observation.",
        "`error: unknown tool 'name'`",
        "`error: TypeError` from trying to call `None`",
        "`no matches` from the archival store"
      ],
      "correct": 1,
      "explanation": "A missing attribute is handled before invocation, and the method appends the formatted unknown-tool error to its observations."
    }
  ]
}
