{
  "lesson": "14-autogen-actor-model",
  "title": "AutoGen v0.4: Actor Model and Agent Framework",
  "questions": [
    {
      "stage": "pre",
      "question": "After `python3 code/main.py` runs the review flow, what does the checklist report?",
      "options": [
        "Consensus is `False`, and one failed reviewer message is in the dead-letter queue.",
        "Consensus is `True`, and the queue is empty because failed handlers are retried.",
        "Consensus is `False`, but the crash stops all three review messages from running.",
        "Consensus is `True`, with the crashed message delivered to the checklist instead."
      ],
      "correct": 0,
      "explanation": "The checklist receives the three review results and reaches `False`; the reviewer handling `crash_me` fails once, so that message is recorded as the single dead letter while the other messages continue."
    },
    {
      "stage": "check",
      "question": "What happens when `Runtime.send` targets an actor name that is not registered?",
      "options": [
        "The runtime creates a temporary actor and invokes its default handler.",
        "It records a dead letter with reason `no actor 'name'` and does not deliver the message.",
        "It blocks the FIFO queue until an actor with that name is registered.",
        "It silently drops the message without changing the trace or dead-letter list."
      ],
      "correct": 1,
      "explanation": "`run_until_idle` looks up the recipient; an unknown name produces a dead letter whose reason is `no actor 'name'`, rather than a delivery or retry."
    },
    {
      "stage": "check",
      "question": "What two guarantees does `Runtime.send` provide for a normal message?",
      "options": [
        "It reuses the sender's ID and dispatches immediately in a nested call.",
        "It assigns a random UUID and processes the message before returning.",
        "It assigns increasing IDs such as `m001` and appends messages to the FIFO queue.",
        "It assigns IDs only after the handler succeeds and reverses the queue for priority."
      ],
      "correct": 2,
      "explanation": "`send` increments the runtime counter, writes the resulting `mNNN` ID into the message, and appends it to the `deque`; processing happens later."
    },
    {
      "stage": "check",
      "question": "How does the runtime isolate a handler that raises `RuntimeError`?",
      "options": [
        "It rolls back every earlier message and re-raises the exception to the caller.",
        "It marks the actor permanently inactive and discards all messages still queued.",
        "It converts the exception to a successful result so the actor's sender can continue.",
        "It catches the exception, records a failed dead letter and trace entry, then continues with queued messages."
      ],
      "correct": 3,
      "explanation": "The handler call is wrapped in a `try`/`except`; the failed message is dead-lettered and traced as `FAIL`, while the FIFO loop proceeds with remaining work."
    },
    {
      "stage": "post",
      "question": "What verdict does the reviewer give a snippet containing `eval(`?",
      "options": [
        "`ok=False` with the issue `uses eval`.",
        "`ok=True` because the snippet has no bare `except:` clause.",
        "`ok=False` with only the issue `missing type annotations`.",
        "The runtime sends it to the dead-letter queue before the reviewer sees it."
      ],
      "correct": 0,
      "explanation": "The review handler checks the snippet text for `eval(` and returns a failed result whose issue list contains `uses eval`."
    },
    {
      "stage": "post",
      "question": "If `runtime.max_messages` is set to `1` while two valid messages are queued, what remains after `run_until_idle()`?",
      "options": [
        "Both messages run because the limit applies only to failed deliveries.",
        "The first message is processed and the second remains in the FIFO queue.",
        "Neither message runs because a bounded run requires an empty queue first.",
        "The second message runs first because bounded runs use LIFO ordering."
      ],
      "correct": 1,
      "explanation": "The loop stops once one message has been processed; with two valid queued messages, the first is handled and the second stays queued for a later call."
    }
  ]
}
