{
  "lesson": "34-repo-memory-and-state",
  "title": "Repo Memory and Durable State",
  "questions": [
    {
      "stage": "pre",
      "question": "What state does the demo persist after selecting the first board task?",
      "options": [
        "`active_task_id` is null and `next_action` is empty.",
        "`active_task_id` is `T-002` and the board has no owner.",
        "`active_task_id` is `T-001` and `next_action` is `read existing /signup handler`.",
        "The state is rejected because `T-001` is not a valid ID."
      ],
      "correct": 2,
      "explanation": "The demo commits the initial state, loads the board's first ID `T-001`, changes the next action, and commits the valid state again."
    },
    {
      "stage": "check",
      "question": "Why does committing `active_task_id=\"T-bogus\"` raise `SchemaError`?",
      "options": [
        "The state schema forbids all string task IDs.",
        "Atomic writes reject values that are not already on disk.",
        "Only board IDs may contain a hyphen.",
        "The task ID does not match the required `T-` plus at least three digits pattern."
      ],
      "correct": 3,
      "explanation": "The `active_task_id` pattern is `^(T-\\d{3,}|)$`; `T-bogus` fails that pattern during `StateManager.commit` validation."
    },
    {
      "stage": "check",
      "question": "What does `StateManager.commit` do before replacing the target file?",
      "options": [
        "It validates the value against the schema, then calls `atomic_write`.",
        "It writes the value first and validates only after the rename.",
        "It silently drops unexpected fields so validation cannot fail.",
        "It loads a board schema even when committing agent state."
      ],
      "correct": 0,
      "explanation": "`commit` calls `validate(state, self.schema)` first; only a valid value reaches `atomic_write`, which prevents a bad write from replacing the file."
    },
    {
      "stage": "check",
      "question": "Which value is accepted for a task-board `owner`?",
      "options": [
        "`operator`",
        "`reviewer`",
        "`agent`",
        "`maintainer`"
      ],
      "correct": 1,
      "explanation": "`BOARD_SCHEMA` restricts `owner` to the enum `builder`, `reviewer`, or `human`; `reviewer` is one of the accepted values."
    },
    {
      "stage": "post",
      "question": "How does `atomic_write` make a state update safer?",
      "options": [
        "It appends a second JSON document so the old value remains readable.",
        "It writes directly to the target and relies on the process not being interrupted.",
        "It writes and fsyncs a temporary file in the target directory, then uses `os.replace`.",
        "It stores the state only in memory until the next Python process starts."
      ],
      "correct": 2,
      "explanation": "The function creates a same-directory temporary file, flushes and fsyncs it, then atomically replaces the destination with `os.replace`."
    },
    {
      "stage": "post",
      "question": "Which status is allowed by the task-board schema even though the demo does not use it?",
      "options": [
        "`paused`",
        "`queued`",
        "`running`",
        "`blocked`"
      ],
      "correct": 3,
      "explanation": "The board status enum is `todo`, `in_progress`, `done`, or `blocked`; `blocked` is valid even though the seeded board starts with `todo`."
    }
  ]
}
