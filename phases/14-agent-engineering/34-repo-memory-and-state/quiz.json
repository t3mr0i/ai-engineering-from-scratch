{
  "lesson": "34-repo-memory-and-state",
  "title": "Repo Memory and Durable State",
  "questions": [
    {
      "stage": "pre",
      "question": "What is the durability test that decides whether a piece of information belongs in repo memory?",
      "options": [
        "Whether it is valid JSON that parses cleanly under the repo's existing schema validator every time",
        "Whether the user explicitly marked the information as important during the chat session",
        "Whether it would be useful three months from now in a CI rerun; if yes, repo; if no, telemetry",
        "Whether the state file fits within a 4 kB budget before compression is applied"
      ],
      "correct": 2,
      "explanation": "Repo memory is for durable, three-months-from-now-useful state; transient data is telemetry."
    },
    {
      "stage": "check",
      "question": "What schema field carries the agent's contract version?",
      "options": [
        "schema_version",
        "manifest_version",
        "runtime_version",
        "config_version"
      ],
      "correct": 0,
      "explanation": "schema_version is the integer contract; the manager refuses to load from an unknown version."
    },
    {
      "stage": "check",
      "question": "How does atomic write work?",
      "options": [
        "Truncate the target file in place, then write the new bytes directly over the old contents",
        "Append each new state update to the end of the file, guarded by a trailing CRC checksum",
        "Encrypt the new state payload and overwrite the target file in place before releasing the file lock",
        "tempfile.mkstemp in the same directory, write, fsync, os.replace (atomic rename) over the target"
      ],
      "correct": 3,
      "explanation": "Atomic rename on POSIX and Windows is what prevents partial-write corruption."
    },
    {
      "stage": "check",
      "question": "Per the lesson's repo-memory table, which of these does NOT belong in repo memory?",
      "options": [
        "The active task id, tracked so the next session knows where to resume",
        "Raw chat transcripts, which stay in the conversation, not the repo",
        "Any open blockers, tracked so nobody re-discovers a known problem",
        "The next action, tracked so the next session has a concrete start"
      ],
      "correct": 1,
      "explanation": "The lesson's table lists raw chat transcripts under 'Does not belong'; active task id, open blockers, and next action all belong in repo memory."
    },
    {
      "stage": "post",
      "question": "What does the lesson say schema-first state buys you?",
      "options": [
        "It lets the agent skip validation once the schema has been defined once",
        "It compresses the state file so checkpoints load faster across sessions",
        "Without a schema, agents invent fields; with one, bad writes are refused",
        "It replaces atomic writes, since the schema already guarantees durability"
      ],
      "correct": 2,
      "explanation": "The lesson says: without a schema, every agent invents new fields, every reviewer learns a new shape, and every CI script has to special-case past versions; with it, a bad write is a refused write."
    },
    {
      "stage": "post",
      "question": "What four things does the schema constrain, per the lesson?",
      "options": [
        "File size limits, text encoding, line endings, and commit message format",
        "The model provider, token budget, temperature, and system prompt used",
        "Branch name, commit author, timestamp format, and total diff size",
        "Required keys, allowed statuses, forbidden values, and pattern rules"
      ],
      "correct": 3,
      "explanation": "The lesson lists what the schema covers: required keys, allowed status values, forbidden values (e.g. null for arrays), and pattern constraints (task ids match T-\\d{3,})."
    }
  ]
}
