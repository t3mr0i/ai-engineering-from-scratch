{
  "lesson": "42-agent-workbench-capstone",
  "title": "Capstone: Ship a Reusable Agent Workbench Pack",
  "questions": [
    {
      "stage": "pre",
      "question": "What does the capstone produce?",
      "options": [
        "A research paper",
        "A versioned drop-in directory (agent-workbench-pack/) with the seven surfaces plus a bin/install.sh that lays them down idempotently",
        "A new LLM",
        "A monitoring dashboard"
      ],
      "correct": 1,
      "explanation": "The pack is the recipe; each install is a serving."
    },
    {
      "stage": "pre",
      "question": "Which is NOT part of the pack layout?",
      "options": [
        "AGENTS.md + docs/",
        "schemas/",
        "scripts/",
        "vendor_proprietary_weights/"
      ],
      "correct": 3,
      "explanation": "Pack layout is AGENTS.md, docs/, schemas/, scripts/, bin/, README.md. The pack is framework- and vendor-agnostic."
    },
    {
      "stage": "check",
      "question": "What does the lesson say bumps the pack's major version?",
      "options": [
        "Any change at all, since the pack has no separate patch tier",
        "Only changes to bin/install.sh, since it is the sole entry point",
        "Adding a new doc file, since docs are the pack's main contract",
        "Schema bumps and script changes that require a migration script"
      ],
      "correct": 3,
      "explanation": "The lesson says schema bumps and script changes that require migrations bump the major version, and doc-only changes bump the patch."
    },
    {
      "stage": "check",
      "question": "What does the lesson say the installer refuses to do by default?",
      "options": [
        "Copy the pack into a repo that has no .github/workflows/ directory",
        "Run at all without an internet connection to fetch schema updates",
        "Install into a repo where agent_state.json does not already exist",
        "Install over an existing pack unless the operator passes --force"
      ],
      "correct": 3,
      "explanation": "The lesson lists the installer's first step as: refuses to install over an existing pack without --force."
    },
    {
      "stage": "check",
      "question": "What three things does the lesson list as staying 'in' the pack?",
      "options": [
        "The target repo's tasks, its CI secrets, and its onboarding wiki",
        "A pinned model ID, a vendor SDK key, and a billing dashboard link",
        "The installer log file, a changelog, and a support contact list",
        "The surface schemas, the four scripts, and the four docs"
      ],
      "correct": 3,
      "explanation": "The lesson's 'what stays in' list is: surface schemas (the contract), the four scripts (the runtime), the four docs (the rules and the rubric)."
    },
    {
      "stage": "post",
      "question": "What stays OUT of the pack?",
      "options": [
        "Schemas",
        "Project-specific tasks, vendor SDK calls, team onboarding prose — the pack is framework-agnostic and lives next to onboarding, not inside it",
        "Scripts",
        "The installer"
      ],
      "correct": 1,
      "explanation": "Tasks belong on the target repo's board, not the pack; vendor SDK calls would lock the pack to one framework."
    },
    {
      "stage": "post",
      "question": "What does the lesson say the installer prints after copying the pack into a target repo?",
      "options": [
        "A checksum of every file it copied, for audit purposes",
        "A list of every AI agent the pack is compatible with",
        "A link to the pack's changelog and version history",
        "Next: fill the board, set commands, run init script"
      ],
      "correct": 3,
      "explanation": "The lesson's installer steps end with: prints next steps: fill in the board, set acceptance commands, run the init script."
    }
  ]
}
