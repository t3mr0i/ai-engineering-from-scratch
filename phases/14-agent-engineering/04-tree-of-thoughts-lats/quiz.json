{
  "lesson": "04-tree-of-thoughts-lats",
  "title": "Tree of Thoughts and LATS: Deliberate Search",
  "questions": [
    {
      "stage": "pre",
      "question": "An assistant reasons step by step toward a plan, but its first step is wrong, so every later step builds on a bad premise. What is the fix?",
      "options": [
        "Let it walk further in the same wrong direction",
        "Ask it to write the wrong plan more confidently",
        "Remove all intermediate steps and guess the answer",
        "Propose several candidate steps, score them, and backtrack from dead ends"
      ],
      "correct": 3,
      "explanation": "A linear walk cannot recover from an early mistake. This lesson builds deliberate search: branch, evaluate, and backtrack."
    },
    {
      "stage": "check",
      "question": "What does `evaluate(4, \"/\", 0)` return?",
      "options": [
        "`0`",
        "`None`",
        "`inf`",
        "It raises `ZeroDivisionError`."
      ],
      "correct": 1,
      "explanation": "The division branch explicitly returns `None` when `b == 0`, allowing `expand` to skip that child."
    },
    {
      "stage": "check",
      "question": "What value does `value(Node(state=(24,), trace=[]))` return?",
      "options": [
        "`0.0`",
        "`0.24`",
        "`1.0`",
        "`-0.24`"
      ],
      "correct": 2,
      "explanation": "A one-element state exactly equal to `TARGET` receives the terminal reward `1.0`."
    },
    {
      "stage": "check",
      "question": "What does `uct(parent, child)` return for an unvisited child?",
      "options": [
        "The parent value `Q` only",
        "Zero, so unvisited children are ignored",
        "The child state distance from 24",
        "Positive infinity, forcing the child to be explored"
      ],
      "correct": 3,
      "explanation": "The first branch of `uct` returns `float(\"inf\")` when `child.visits == 0`."
    },
    {
      "stage": "post",
      "question": "After `backprop([root, child], 1.0)`, what changes on both nodes?",
      "options": [
        "Each gets `visits += 1` and `value_sum += 1.0`.",
        "Only `root.visits` changes; child statistics are recomputed later.",
        "Each state is replaced by `(24,)` and its trace is cleared.",
        "The reward is divided by depth before it reaches the root."
      ],
      "correct": 0,
      "explanation": "`backprop` loops over every node in the path and adds one visit plus the supplied reward to each node."
    },
    {
      "stage": "post",
      "question": "When does `tot_bfs` return early instead of scoring the remaining frontier?",
      "options": [
        "When the frontier contains exactly eight nodes.",
        "When a child has `value(child) > 0.99`.",
        "When a division operation returns `None`.",
        "When `max_depth` reaches zero before expansion."
      ],
      "correct": 1,
      "explanation": "The implementation checks each expanded child and immediately returns it when its value exceeds `0.99`, which denotes an exact target."
    }
  ]
}
