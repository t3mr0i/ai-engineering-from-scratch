{
  "lesson": "45-prompt-only-vs-rules-first",
  "title": "Prompt-Only vs Rules-First",
  "questions": [
    {
      "stage": "pre",
      "question": "Which changed_files tuple does prompt_only_attempt return for the validator task?",
      "options": [
        "('src/validator.py', 'tests/test_validator.py')",
        "('src/validator.py', 'README.md')",
        "('src/validator.py', 'tests/test_validator.py', 'notes.txt')",
        "('notes.txt', 'README.md', 'src/validator.py')"
      ],
      "correct": 2,
      "explanation": "The baseline deliberately includes notes.txt in addition to the source and test paths, creating an out-of-scope violation."
    },
    {
      "stage": "check",
      "question": "Which required check is missing from the prompt-only attempt?",
      "options": [
        "unit",
        "format",
        "lint",
        "acceptance"
      ],
      "correct": 3,
      "explanation": "The task requires unit and acceptance, but prompt_only_attempt reports completed_checks=('unit',) only."
    },
    {
      "stage": "check",
      "question": "What changed_files does rules_first_attempt use?",
      "options": [
        "Exactly task.allowed_files",
        "The prompt-only tuple plus notes.txt",
        "Every file under src/ and tests/",
        "Only the first allowed path"
      ],
      "correct": 0,
      "explanation": "rules_first_attempt returns changed_files=task.allowed_files and creates an artifact entry for each of those paths."
    },
    {
      "stage": "check",
      "question": "Which artifact entries are missing from the prompt-only attempt?",
      "options": [
        "src/validator.py only",
        "tests/test_validator.py and notes.txt",
        "No entries; the note claims the task is ready",
        "src/validator.py and tests/test_validator.py"
      ],
      "correct": 1,
      "explanation": "The baseline artifact contains only src/validator.py; validate_attempt therefore reports missing entries for the test path and notes.txt."
    },
    {
      "stage": "post",
      "question": "Which violation does validate_attempt report first for the prompt-only attempt?",
      "options": [
        "missing checks: acceptance",
        "missing artifact entries: notes.txt, tests/test_validator.py",
        "out-of-scope files: notes.txt",
        "invalid task goal"
      ],
      "correct": 2,
      "explanation": "validate_attempt checks the changed-path set first, so the sorted out-of-scope receipt for notes.txt precedes missing checks and artifacts."
    },
    {
      "stage": "post",
      "question": "What verdict does validate_attempt return for rules_first_attempt on the same Task?",
      "options": [
        "passed=False because the note is not evidence",
        "passed=False because every artifact is only a fixture string",
        "passed=False because the unit check is not rerun inside the validator",
        "passed=True with no violations"
      ],
      "correct": 3,
      "explanation": "The rules-first attempt stays within allowed_files, lists both required checks, and supplies an artifact entry for each changed path."
    }
  ]
}
