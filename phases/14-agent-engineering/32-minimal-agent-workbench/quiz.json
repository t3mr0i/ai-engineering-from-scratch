{
  "lesson": "32-minimal-agent-workbench",
  "title": "The Minimal Agent Workbench",
  "questions": [
    {
      "stage": "pre",
      "question": "After the demo completes its current turn, what durable state is shown?",
      "options": [
        "`T-001` is still active and waiting for its test command.",
        "Both `T-001` and `T-002` are marked done.",
        "No active task; `T-001` is done, `T-002` remains todo, and the next action is to pick the next task.",
        "The board is empty because completed tasks are deleted."
      ],
      "correct": 2,
      "explanation": "With both touched files already recorded, `run_one_turn` closes `T-001`, clears `active_task_id` and `touched_files`, and leaves `T-002` for the next turn."
    },
    {
      "stage": "check",
      "question": "What happens when `run_one_turn` starts with `active_task_id=None` and a todo board?",
      "options": [
        "It marks every todo task done without selecting one.",
        "It sets the state to idle and leaves all tasks todo.",
        "It raises because a state file must always name an active task.",
        "It selects the first todo task, marks it `in_progress`, and records it as active."
      ],
      "correct": 3,
      "explanation": "The method uses `next` over board tasks with status `todo`; the selected task becomes `in_progress`, and its ID is stored in `active_task_id`."
    },
    {
      "stage": "check",
      "question": "How does `run_one_turn` recover when the active task ID is absent from the board?",
      "options": [
        "It clears `active_task_id` and sets `next_action` to reset and pick new work.",
        "It creates a new task with the missing ID.",
        "It marks the whole board blocked and stops permanently.",
        "It silently keeps the stale ID so a later turn can find it."
      ],
      "correct": 0,
      "explanation": "If the lookup returns `None`, the implementation resets `active_task_id` and records `active task missing from board; resetting and picking new work`."
    },
    {
      "stage": "check",
      "question": "What is the first work action for an active task whose state has no `app.py` entry?",
      "options": [
        "Mark the task done and clear the board.",
        "Append `app.py` to `touched_files` and set the next action to add its acceptance test.",
        "Run the verification command before touching any file.",
        "Create a new `T-003` task instead of modifying state."
      ],
      "correct": 1,
      "explanation": "The first active-task branch appends `app.py` when it is absent and sets `next_action` to `add test for <active id> acceptance`."
    },
    {
      "stage": "post",
      "question": "What happens when an active task already lists both `app.py` and `test_app.py` as touched?",
      "options": [
        "It remains `in_progress` until another session opens the board.",
        "It adds a third touched file for the verification report.",
        "It marks the task `done`, clears the active task and touched files, and points to the next board task.",
        "It deletes the task's acceptance command from the board."
      ],
      "correct": 2,
      "explanation": "The final branch sets `active.status = \"done\"`, clears the active ID and touched list, and sets `next_action` to `pick next task from board`."
    },
    {
      "stage": "post",
      "question": "What does the generated `AGENTS.md` call the definition of done?",
      "options": [
        "The agent has written at least one source file.",
        "The task has a non-empty chat transcript.",
        "The board contains no todo tasks, regardless of tests.",
        "The active task is `done` on the board and its listed verification command exits 0."
      ],
      "correct": 3,
      "explanation": "The router says completion requires the task referenced by `agent_state.active_task_id` to be `done` and its acceptance verification command to exit zero."
    }
  ]
}
