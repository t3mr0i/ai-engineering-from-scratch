{
  "lesson": "11-planning-htn-and-evolutionary",
  "title": "Planning with HTN and Evolutionary Search",
  "questions": [
    {
      "stage": "pre",
      "question": "What condition must hold for `Operator.applicable(state)` to return `True`?",
      "options": [
        "At least one precondition appears in `state`.",
        "The operator has no effects to apply.",
        "Every precondition appears in `state`.",
        "The LLM has already proposed the operator in its cache."
      ],
      "correct": 2,
      "explanation": "The method returns `all(p in state for p in self.preconditions)`, so every declared precondition is required."
    },
    {
      "stage": "check",
      "question": "How does `Operator.apply` change its input state?",
      "options": [
        "It mutates the original set while deleting all unrelated facts.",
        "It returns the original set unchanged and records effects in the LLM.",
        "It replaces the state with only `effects_add`.",
        "It copies the set, discards `effects_remove`, and adds `effects_add`."
      ],
      "correct": 3,
      "explanation": "`apply` starts with `new_state = set(state)`, then removes declared negative effects and adds positive effects."
    },
    {
      "stage": "check",
      "question": "What plan does the built-in method for `ship_change` produce from `{\"logged_in\"}`?",
      "options": [
        "`open_editor`, `write_tests`, `run_tests`, `open_pr`",
        "`write_tests`, `open_editor`, `open_pr`, `run_tests`",
        "`open_editor`, `run_tests`, `write_tests`, `open_pr`",
        "No plan, because `ship_change` requires `tests_passing` initially."
      ],
      "correct": 0,
      "explanation": "The matching method decomposes the task in that order, and each operator's effects satisfy the next operator's preconditions."
    },
    {
      "stage": "check",
      "question": "What happens when `HTNPlanner.plan` sees `ship_feature_with_migration`, which has no stored method initially?",
      "options": [
        "It returns an empty list without consulting the LLM.",
        "It asks `ScriptedLLM` for a decomposition, validates the names, caches it, and expands it.",
        "It directly executes the first suggested operator without checking preconditions.",
        "It raises `KeyError` because unknown compound tasks are forbidden."
      ],
      "correct": 1,
      "explanation": "The no-method branch calls `llm.decompose`, rejects unknown symbols, stores the valid tuple in `cached_methods`, and expands it symbolically."
    },
    {
      "stage": "post",
      "question": "What happens on the second `ship_feature_with_migration` planning call?",
      "options": [
        "The planner clears the cache and asks the LLM for a different method.",
        "The planner returns `None` because cached plans are single-use.",
        "The cached decomposition is used, so the LLM call count increases by zero.",
        "The planner skips operator preconditions because the plan was already validated."
      ],
      "correct": 2,
      "explanation": "The scripted demo computes `new_calls` as zero: the cached tuple is expanded directly on the second call."
    },
    {
      "stage": "post",
      "question": "What candidate minimizes the evolutionary demo's deterministic fitness function?",
      "options": [
        "`a=0, b=7`, because the intercept dominates the target",
        "`a=2, b=3`, because it is the first seeded survivor",
        "`a=3, b=0`, because only the slope is evaluated",
        "`a=3, b=7`, with fitness `0.00`"
      ],
      "correct": 3,
      "explanation": "The evaluator compares `a*x+b` to `3*x+7` for x from -5 through 5; the exact pair `(3, 7)` has zero squared error."
    }
  ]
}
