{
  "lesson": "20-fourier-transform",
  "title": "The Fourier Transform",
  "questions": [
    {
      "stage": "pre",
      "question": "What DFT coefficients result from the constant signal [1,1,1,1]?",
      "options": [
        "X[0]=4 and X[1]=X[2]=X[3]=0",
        "All four coefficients equal 1",
        "X[0]=0 and X[1]=4",
        "Every coefficient has magnitude 4"
      ],
      "correct": 0,
      "explanation": "The zero-frequency coefficient sums all samples. The rotating factors cancel at nonzero frequencies for a constant signal."
    },
    {
      "stage": "check",
      "question": "What does the coefficient X[0] represent for a real signal under this DFT convention?",
      "options": [
        "The highest oscillation frequency",
        "The DC component, equal to the sum and proportional to the mean",
        "Only the imaginary part of the signal",
        "The phase of the Nyquist component"
      ],
      "correct": 1,
      "explanation": "At k=0 every factor is 1, so X[0] is the sum of samples. Dividing by N gives the mean under the usual normalization."
    },
    {
      "stage": "check",
      "question": "Which sign and scale reconstruct the signal in the inverse DFT?",
      "options": [
        "Use the same sign and multiply by N",
        "Discard all negative frequencies with no conjugate symmetry",
        "Use the opposite exponential sign and divide the sum by N",
        "Take the absolute value of each coefficient"
      ],
      "correct": 2,
      "explanation": "The forward transform uses a negative sign in the exponent; the inverse uses a positive sign and the 1/N normalization."
    },
    {
      "stage": "check",
      "question": "How does the convolution theorem compute a discrete convolution with FFTs?",
      "options": [
        "Add the two spectra and take a second forward FFT",
        "Multiply the time-domain samples only at equal indices",
        "Take the determinant of the two Fourier matrices",
        "FFT each input, multiply the spectra pointwise, then apply the inverse FFT"
      ],
      "correct": 3,
      "explanation": "Circular convolution in the time domain corresponds to pointwise multiplication in frequency. Padding handles linear convolution when required."
    },
    {
      "stage": "post",
      "question": "For N=8 samples, which bin is the Nyquist frequency?",
      "options": [
        "k=4",
        "k=1",
        "k=7",
        "k=8"
      ],
      "correct": 0,
      "explanation": "The Nyquist bin is N/2 for even N, so k=4. Frequencies above it alias into lower frequencies."
    },
    {
      "stage": "post",
      "question": "Why is an FFT preferable to the direct DFT for a long sequence?",
      "options": [
        "The FFT removes the need for complex arithmetic",
        "Cooley-Tukey reduces typical work from O(N^2) to O(N log N)",
        "The DFT cannot represent phase",
        "The FFT always returns exact real numbers"
      ],
      "correct": 1,
      "explanation": "The FFT reuses subproblems in the DFT sum. It preserves the same frequency information while reducing computational cost for suitable lengths."
    }
  ]
}
