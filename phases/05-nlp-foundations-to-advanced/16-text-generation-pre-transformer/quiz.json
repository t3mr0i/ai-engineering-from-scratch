{
  "lesson": "16-text-generation-pre-transformer",
  "title": "Text Generation Before Transformers — N-gram Language Models",
  "questions": [
    {
      "stage": "pre",
      "question": "Which boundary tokens does train_bigrams add to every sentence?",
      "options": [
        "<BOS> at both ends",
        "A period at the start and a comma at the end",
        "<s> at the start and </s> at the end",
        "No boundary tokens; it counts only interior words"
      ],
      "correct": 2,
      "explanation": "The padded sequence is [\"<s>\"] + sentence + [\"</s>\"], so starts and stops receive bigram counts."
    },
    {
      "stage": "check",
      "question": "What does laplace_prob return for an unseen bigram when count=0?",
      "options": [
        "Exactly zero",
        "The probability of the previous word",
        "The Kneser-Ney continuation probability",
        "A positive smoothed probability 1/(unigram_count(prev)+vocab_size)"
      ],
      "correct": 3,
      "explanation": "Add-one smoothing changes the numerator from zero to one while retaining the vocabulary-sized denominator adjustment."
    },
    {
      "stage": "check",
      "question": "What does Kneser-Ney continuation count for a word w?",
      "options": [
        "The number of distinct preceding contexts of w divided by total distinct bigrams",
        "The raw number of times w appears in all documents",
        "The number of characters in w divided by vocabulary size",
        "Only the most frequent predecessor of w"
      ],
      "correct": 0,
      "explanation": "unigram_contexts[w] stores distinct predecessors, and the code divides its size by total_unique_bigrams."
    },
    {
      "stage": "check",
      "question": "How does perplexity convert the accumulated log probabilities?",
      "options": [
        "It averages raw probabilities without taking logs",
        "It returns exp(-total_log / total), the exponential of mean negative log probability",
        "It counts the number of unique words in each sentence",
        "It returns the highest next-word probability"
      ],
      "correct": 1,
      "explanation": "The function sums log p over predicted tokens, divides by token count, negates, and exponentiates."
    },
    {
      "stage": "post",
      "question": "Why does sample_sentence exclude \"<s>\" from its candidate words?",
      "options": [
        "The start marker is always the most probable semantic word",
        "The model cannot store a start marker in its vocabulary",
        "The start marker should begin the sequence, not be generated as an interior word",
        "Excluding it forces every sentence to end at the first token"
      ],
      "correct": 2,
      "explanation": "The candidate list skips <s>; generation can still select </s> and then returns words between the markers."
    },
    {
      "stage": "post",
      "question": "What failure does Laplace smoothing prevent for an unseen test bigram?",
      "options": [
        "A document length longer than the training corpus",
        "A token being assigned two POS tags",
        "A duplicate sentence being sampled twice",
        "A zero probability that would make the sentence log probability undefined"
      ],
      "correct": 3,
      "explanation": "Giving unseen transitions a small positive probability avoids log(0) and infinite perplexity for an otherwise evaluable sentence."
    }
  ]
}
