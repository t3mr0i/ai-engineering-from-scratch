{
  "lesson": "17-self-supervised-vision",
  "title": "Self-Supervised Vision — SimCLR, DINO, MAE",
  "questions": [
    {
      "stage": "pre",
      "question": "For z1 and z2 shaped (16,32), how many rows are in the similarity matrix inside info_nce?",
      "options": [
        "32",
        "16",
        "64",
        "2"
      ],
      "correct": 0,
      "explanation": "info_nce concatenates the two views along the batch axis, so z has 2N=32 rows and sim is 32 by 32."
    },
    {
      "stage": "check",
      "question": "For the first view row i in info_nce, where is its positive target located?",
      "options": [
        "At column i in the same view",
        "At column i+N in the second view",
        "At the diagonal column i, which is masked",
        "At the final column for every row"
      ],
      "correct": 1,
      "explanation": "The targets concatenate [N,...,2N-1] for the first half and [0,...,N-1] for the second, pairing the two augmentations."
    },
    {
      "stage": "check",
      "question": "With num_patches=196 and mask_ratio=0.75, how many patches does random_mask_indices keep visible?",
      "options": [
        "147",
        "75",
        "49",
        "196"
      ],
      "correct": 2,
      "explanation": "n_keep=int(196*(1-0.75))=49; the remaining 147 indices are masked."
    },
    {
      "stage": "check",
      "question": "What is the effect of lowering tau in info_nce while similarities stay fixed?",
      "options": [
        "It removes the positive pair from the target list",
        "It turns the loss into pixel-wise MSE",
        "It makes every row of sim identical",
        "It sharpens the similarity logits, emphasizing the relative positive-versus-negative gap"
      ],
      "correct": 3,
      "explanation": "The code divides similarities by tau before cross-entropy; a smaller temperature makes high and low similarities more separated."
    },
    {
      "stage": "post",
      "question": "How does the toy DINO teacher differ from the student output?",
      "options": [
        "The teacher centers and sharpens with softmax, then detaches its output",
        "The teacher uses argmax labels and backpropagates through them",
        "The student is centered but the teacher returns raw RGB pixels",
        "Both branches always use the same temperature and gradients"
      ],
      "correct": 0,
      "explanation": "teacher subtracts centre, divides by a lower temperature, applies softmax, and calls detach; student returns log_softmax with its own temperature."
    },
    {
      "stage": "post",
      "question": "Which objective family matches each self-supervised example in the lesson?",
      "options": [
        "SimCLR reconstructs pixels, DINO detects boxes, and MAE uses class labels",
        "SimCLR contrasts two views, DINO matches teacher/student views, and MAE reconstructs masked patches",
        "All three optimize the same supervised cross-entropy target",
        "MAE requires a discriminator while SimCLR requires optical flow"
      ],
      "correct": 1,
      "explanation": "The three families use different pretext signals: instance-level contrast, teacher-student consistency, and masked reconstruction."
    }
  ]
}
