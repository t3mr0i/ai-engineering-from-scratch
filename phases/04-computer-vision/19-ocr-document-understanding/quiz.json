{
  "lesson": "19-ocr-document-understanding",
  "title": "OCR & Document Understanding",
  "questions": [
    {
      "stage": "pre",
      "question": "Which index is the CTC blank symbol in the lesson's VOCAB?",
      "options": [
        "0",
        "1",
        "The final vocabulary index",
        "It has no index"
      ],
      "correct": 0,
      "explanation": "VOCAB starts with '_' at index zero, followed by digits and lowercase letters."
    },
    {
      "stage": "check",
      "question": "What shape does TinyCRNN.forward return for a batch of N line images?",
      "options": [
        "(N,1,H,W), the input image reconstructed",
        "(T,N,V), log probabilities over the vocabulary at each time",
        "(N,V,T,H), one class map per pixel",
        "(V,N), one token for the whole batch"
      ],
      "correct": 1,
      "explanation": "The CNN features are arranged as batch-time-feature, the bidirectional LSTM runs with batch_first, and the head is transposed to CTC's time-batch-class layout."
    },
    {
      "stage": "check",
      "question": "What does greedy_ctc_decode do with repeated non-blank IDs such as [3,3,0,4]?",
      "options": [
        "Produces [3,3,0,4] unchanged",
        "Sorts the IDs numerically to [0,3,3,4]",
        "Collapses the repeat and blank to produce [3,4]",
        "Drops only the final ID"
      ],
      "correct": 2,
      "explanation": "CTC decoding emits an ID only when it differs from the previous ID and is not blank, so repeated characters are collapsed."
    },
    {
      "stage": "check",
      "question": "Why does build_batch return target_lengths separately from the concatenated targets tensor?",
      "options": [
        "The lengths are used as image channel counts",
        "They tell the CNN how many filters to create",
        "CTC ignores the target IDs and uses only lengths",
        "CTC needs each string's length to split the flattened target sequence for the batch"
      ],
      "correct": 3,
      "explanation": "The targets are concatenated into one vector, while target_lengths tells F.ctc_loss where each example's target ends."
    },
    {
      "stage": "post",
      "question": "How should zero_infinity=True be used with the lesson's CTC length checks?",
      "options": [
        "Validate alignment feasibility first; zero_infinity is only a numerical safeguard, not permission to accept impossible examples",
        "Use it to make every character probability exactly zero",
        "Use it to remove the blank symbol from the vocabulary",
        "Use it to force input and target lengths to be identical"
      ],
      "correct": 0,
      "explanation": "The implementation rejects input lengths below target_length plus adjacent-repeat_count before calling the Torch loss. zero_infinity=True remains a downstream numerical guard and does not validate sequence feasibility."
    },
    {
      "stage": "post",
      "question": "Which stages distinguish OCR from broader document understanding?",
      "options": [
        "OCR only detects boxes, while document understanding only changes image colors",
        "OCR reads text, layout parsing locates structure, and understanding interprets fields or meaning",
        "Layout parsing replaces recognition, so no text decoder is needed",
        "All three tasks are identical pixel classifications"
      ],
      "correct": 1,
      "explanation": "A document system may need detection, character recognition, spatial layout, and semantic interpretation; each answers a different question."
    }
  ]
}
