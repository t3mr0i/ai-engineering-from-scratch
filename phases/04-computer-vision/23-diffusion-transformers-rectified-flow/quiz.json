{
  "lesson": "23-diffusion-transformers-rectified-flow",
  "title": "Diffusion Transformers and Rectified Flow",
  "questions": [
    {
      "stage": "pre",
      "question": "For an NCHW image with shape (1, 3, 16, 16) and patch_size=2, how many patch tokens does patchify create?",
      "options": ["8", "32", "64", "256"],
      "correct": 2,
      "explanation": "There are 16/2 by 16/2 spatial patches: 8*8 = 64 tokens, each carrying 3*2*2 values."
    },
    {
      "stage": "check",
      "question": "What happens to rectified_flow_path(x0, noise, t) at t=0?",
      "options": [
        "It returns noise and zero velocity",
        "It returns x0 and velocity noise-x0",
        "It returns the midpoint and velocity x0-noise",
        "It raises because t must be positive"
      ],
      "correct": 1,
      "explanation": "The path is (1-t)x0+t noise, so t=0 yields x0; its constant velocity remains noise-x0."
    },
    {
      "stage": "check",
      "question": "Why must a patchified image have height and width divisible by patch_size?",
      "options": [
        "The sinusoidal embedding only accepts square images",
        "The RGB channels would otherwise be discarded by softmax",
        "Unpatchify needs complete non-overlapping blocks to restore the original grid",
        "Rectified flow requires an integer noise value"
      ],
      "correct": 2,
      "explanation": "The local contract does not pad or crop. A remainder would make the inverse token ordering ambiguous, so it raises ValueError."
    },
    {
      "stage": "check",
      "question": "What is the velocity for x0=0 and noise=1 in every coordinate?",
      "options": ["0", "-1", "1", "It depends on t"],
      "correct": 2,
      "explanation": "Rectified flow uses the constant difference noise-x0, which is one and does not depend on t."
    },
    {
      "stage": "post",
      "question": "With four Euler steps and velocity 1, what does euler_reverse_sample start at x1=1 and end at?",
      "options": ["0", "0.25", "1", "4"],
      "correct": 0,
      "explanation": "Each reverse step subtracts Δt=1/4; four subtractions move 1 to zero."
    },
    {
      "stage": "post",
      "question": "Which input is rejected by timestep_embedding?",
      "options": [
        "times [0.0, 0.5] with dim=8",
        "times [1.0] with dim=2",
        "times [0.0] with dim=7",
        "times [-0.2] with dim=4"
      ],
      "correct": 2,
      "explanation": "The embedding width must be a positive even integer. Time range validation belongs to the flow path, not this sinusoid helper."
    }
  ]
}
