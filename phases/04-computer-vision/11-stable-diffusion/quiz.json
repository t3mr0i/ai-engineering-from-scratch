{
  "lesson": "11-stable-diffusion",
  "title": "Stable Diffusion: Latents, Guidance, and Honest Boundaries",
  "questions": [
    {
      "stage": "pre",
      "question": "Which sequence best describes a text-conditioned latent diffusion pipeline?",
      "options": [
        "Conditioning, denoising/scheduling in latent space, decoding, then a safety decision",
        "A detector box directly becomes a class label",
        "A semantic mask is resized and no latent state is used",
        "A GAN discriminator replaces both the scheduler and VAE"
      ],
      "correct": 0,
      "explanation": "A latent diffusion pipeline separates conditioning, latent denoising, the VAE image adapter, and an output policy check."
    },
    {
      "stage": "check",
      "question": "What does `latent_shape((2,3,32,32), downsample_factor=8, latent_channels=4)` return?",
      "options": [
        "(2, 3, 4, 4)",
        "(2, 4, 4, 4)",
        "(2, 4, 8, 8)",
        "(1, 4, 32, 32)"
      ],
      "correct": 1,
      "explanation": "The batch is retained, four latent channels are requested, and each spatial axis is divided by eight: 32/8 = 4."
    },
    {
      "stage": "check",
      "question": "With unconditional zeros, conditional ones, and guidance_scale=5, what value does `classifier_free_guidance` return?",
      "options": [
        "0",
        "1",
        "4",
        "5"
      ],
      "correct": 3,
      "explanation": "The formula is 0 + 5*(1-0), so every element is 5. Guidance extrapolates the conditional difference."
    },
    {
      "stage": "check",
      "question": "For `base(4,4)`, `down(2,4)`, and `up(4,2)`, what is the shape of `up @ down`?",
      "options": [
        "(2,2)",
        "(4,2)",
        "(2,4)",
        "(4,4)"
      ],
      "correct": 3,
      "explanation": "The inner rank dimension 2 contracts, leaving the outer dimensions `(4,4)`, which can be added to base."
    },
    {
      "stage": "post",
      "question": "What does this lesson's `pipeline_manifest` establish?",
      "options": [
        "That a released checkpoint generated an image",
        "That each local component has an explicit role and implementation status",
        "That the safety checker approved every prompt",
        "That guidance scale 5 is universally optimal"
      ],
      "correct": 1,
      "explanation": "The manifest is an offline ownership ledger. It explicitly marks the denoiser and safety path as contract-only, so it cannot imply inference."
    },
    {
      "stage": "post",
      "question": "What does `scheduler_sigmas(2, start=1.0, end=0.1)` return, and why is one step rejected?",
      "options": [
        "[1.0, 1.0], because the final sigma is always the start value",
        "[1.0, 0.1], because two endpoints require at least two sequence entries",
        "[0.1, 1.0], because reverse schedules run from low to high sigma",
        "[1.0], because a one-entry schedule can represent both endpoints"
      ],
      "correct": 1,
      "explanation": "`np.linspace` includes both endpoints when there are at least two steps; `scheduler_sigmas(1,...)` is rejected because a single value cannot represent both start and end."
    }
  ]
}
