{
  "lesson": "17-disaggregated-prefill-decode",
  "title": "Disaggregated Prefill/Decode — NVIDIA Dynamo and llm-d",
  "questions": [
    {
      "stage": "pre",
      "question": "GPUs idle during decode after spending everything on prefill, wasting a fifth of GPU time on the wrong resource. What fixes that?",
      "options": [
        "Bigger GPUs for both phases",
        "No batching at all",
        "Shorter prompts by truncation",
        "Split prefill and decode onto pools sized per bottleneck"
      ],
      "correct": 3,
      "explanation": "Colocated phases over-provision both. This lesson disaggregates: compute pool for prefill, bandwidth pool for decode."
    },
    {
      "stage": "check",
      "question": "For 4,096 prompt tokens, how large is the RDMA KV transfer in this toy model?",
      "options": [
        "51.2 KB",
        "5.12 GB",
        "512 GB",
        "0.512 GB KV"
      ],
      "correct": 3,
      "explanation": "4,096 × 125,000 bytes = 512,000,000 bytes = 0.512 GB; at 100 GB/s the transfer takes about 5.12 ms."
    },
    {
      "stage": "check",
      "question": "Which decode rate is used for the memory-optimized disaggregated GPU pool?",
      "options": [
        "0.18 tokens/ms",
        "40 tokens/ms",
        "0.10 tokens/ms",
        "100 GB/s"
      ],
      "correct": 0,
      "explanation": "DECODE_TOK_PER_MS_DECODE_GPU is 0.18; colocated decode uses the slower 0.10 tokens/ms constant."
    },
    {
      "stage": "check",
      "question": "Why is TCP transfer slower than the NIXL RDMA path in ms_disaggregated?",
      "options": [
        "TCP skips the KV transfer entirely",
        "TCP 10 GB/s vs RDMA 100",
        "RDMA adds a second prefill pass",
        "TCP uses a larger decode GPU"
      ],
      "correct": 1,
      "explanation": "The transfer time divides the same KV bytes by the transport bandwidth; TCP's 10 GB/s constant is one tenth of RDMA's 100 GB/s."
    },
    {
      "stage": "post",
      "question": "With output tokens held fixed, what does increasing prompt length do to disaggregated serving?",
      "options": [
        "It removes prefill work from both paths",
        "It makes RDMA bandwidth irrelevant",
        "It increases KV transfer time, weakening the disaggregated advantage",
        "It makes decode throughput independent of output length"
      ],
      "correct": 2,
      "explanation": "With output fixed, prompt tokens add KV_BYTES_PER_TOKEN_70B_FP8 to the transfer term in ms_disaggregated, while the decode-time difference stays fixed; the extra transfer therefore weakens the advantage."
    },
    {
      "stage": "post",
      "question": "What KV size per prompt token does the 70B FP8 fixture use?",
      "options": [
        "16 bytes",
        "1,250 bytes",
        "1,000,000 bytes",
        "125,000 bytes"
      ],
      "correct": 3,
      "explanation": "The constant KV_BYTES_PER_TOKEN_70B_FP8 is 125,000 bytes; multiplying it by prompt gives the transferred KV payload."
    }
  ]
}
