{
  "lesson": "27-finops-llms",
  "title": "FinOps for LLMs — Unit Economics and Multi-Tenant Attribution",
  "questions": [
    {
      "stage": "pre",
      "question": "Why does traditional FinOps break on LLM spend?",
      "options": [
        "LLMs don't cost money",
        "LLM bills are always free",
        "Cloud providers refuse to itemize",
        "Costs are token-transactions rather than resource-uptime; tags don't auto-propagate from API calls and you must stamp user/task/tenant at the call site"
      ],
      "correct": 3,
      "explanation": "The lesson states LLM costs are token-transactions rather than resource-uptime, so cloud-style tags do not auto-propagate from an API call the way they do for EC2 or S3 -- you must stamp user/task/tenant metadata at the call site yourself."
    },
    {
      "stage": "check",
      "question": "Which three attribution dimensions does the lesson require instrumenting on day one?",
      "options": [
        "Per-user (user_id), per-task (task_id + route), per-tenant (tenant_id)",
        "Provider, model, API version",
        "Region, AZ, datacenter",
        "GPU, CPU, RAM"
      ],
      "correct": 0,
      "explanation": "The lesson's three attribution dimensions are per-user (user_id, for seat pricing and expansion), per-task (task_id + route, for product-surface cost), and per-tenant (tenant_id, for unit economics and renewal) -- instrumented at the call site from day one."
    },
    {
      "stage": "check",
      "question": "Which four token layers should be broken out in cost attribution?",
      "options": [
        "Prompt, tool, memory, response",
        "Input, output, network, disk",
        "Cache, model, gateway, observability",
        "GPU, CPU, RAM, storage"
      ],
      "correct": 0,
      "explanation": "The lesson breaks token spend into prompt, tool, memory, and response layers, noting that bucketing all four together makes cost optimization blind to which layer is actually driving spend."
    },
    {
      "stage": "check",
      "question": "What is the kill-switch trigger in the enforcement ladder?",
      "options": [
        "Tenant spend z-score > 4 relative to baseline; auto-pause tenant and page on-call",
        "Latency P50 > 2s",
        "Any 5xx response",
        "Spend over $1 in a minute"
      ],
      "correct": 0,
      "explanation": "The lesson's third rung of the enforcement ladder triggers when a tenant's spend z-score exceeds 4 relative to its own baseline, auto-pausing the tenant and paging on-call, escalating to ops and customer success."
    },
    {
      "stage": "post",
      "question": "Which unit metric does the lesson recommend instead of $/M tokens?",
      "options": [
        "Cost per gateway",
        "Cost per GPU-hour",
        "Cost per second",
        "Cost per product outcome (e.g. cost per resolved support ticket, cost per generated article, cost per successful agent task)"
      ],
      "correct": 3,
      "explanation": "The lesson calls $/M tokens 'vendor speak' and recommends tying cost to a product outcome instead -- cost per resolved support ticket, cost per generated article, cost per successful agent task -- so optimization has an anchor."
    },
    {
      "stage": "post",
      "question": "Which attribution pattern does the lesson call the highest-accuracy one mature teams use?",
      "options": [
        "Sampling and extrapolation",
        "Model-based allocation",
        "Tag-and-aggregate only",
        "Telemetry joiner — join traces to billing via trace IDs"
      ],
      "correct": 3,
      "explanation": "The lesson names the telemetry-joiner pattern -- joining traces to billing via trace IDs -- as the highest-accuracy attribution pattern and what mature teams do, versus the simpler and rougher tag-and-aggregate approach."
    }
  ]
}
