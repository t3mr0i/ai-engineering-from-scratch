{
  "lesson": "08-inference-metrics-goodput",
  "title": "Inference Metrics — TTFT, TPOT, ITL, Goodput, P99",
  "questions": [
    {
      "stage": "pre",
      "question": "In the demo's goodput function, when is one RequestTrace counted as good?",
      "options": [
        "All three SLOs pass",
        "When aggregate throughput exceeds the target",
        "When its mean TPOT is below the limit, regardless of E2E",
        "When at least one of the three latency limits passes"
      ],
      "correct": 0,
      "explanation": "goodput increments only when all three comparisons use <=: ttft_ms, tpot_genaiperf(), and e2e_ms must each satisfy the supplied SLO."
    },
    {
      "stage": "check",
      "question": "For RequestTrace, which terms make up ttft_ms in the local implementation?",
      "options": [
        "Decode time plus output length",
        "queue_ms plus prefill_ms",
        "Only network response time",
        "Prompt tokens divided by output tokens"
      ],
      "correct": 1,
      "explanation": "The ttft_ms property returns queue_ms + prefill_ms; decode samples are added later by e2e_ms."
    },
    {
      "stage": "check",
      "question": "A trace has TTFT 500 ms, 100 output tokens, and 700 ms total decode time. What does tpot_genaiperf return?",
      "options": [
        "12.00 ms",
        "5.00 ms",
        "7.07 ms",
        "About 0.07 ms"
      ],
      "correct": 2,
      "explanation": "GenAI-Perf excludes TTFT and divides the 700 ms decode sum by output_tokens - 1, so 700/99 is about 7.07 ms."
    },
    {
      "stage": "check",
      "question": "For that same trace, what does tpot_llmperf report?",
      "options": [
        "7.07 ms because it starts at token 2",
        "500 ms because TTFT is the only input",
        "0 ms because decode is excluded",
        "e2e / 100 = 12 ms",
      ],
      "correct": 3,
      "explanation": "LLMPerf's local method returns e2e_ms/output_tokens = (500 + 700)/100 = 12.00 ms, including TTFT."
    },
    {
      "stage": "post",
      "question": "Why can high aggregate throughput coexist with poor goodput?",
      "options": [
        "Aggregate speed can hide SLO misses",
        "Goodput ignores all latency constraints",
        "Throughput is always computed from P99 only",
        "A request is good if any single token arrives"
      ],
      "correct": 0,
      "explanation": "Throughput sums output tokens over the fleet; goodput is a per-request all-constraints result, so fast aggregate work can still violate TTFT, TPOT, or E2E."
    },
    {
      "stage": "post",
      "question": "Why does the lesson emphasize P50, P90, and P99 rather than only a mean?",
      "options": [
        "Percentiles remove the need to define TTFT",
        "Tails disappear in the mean"
        "The mean cannot be calculated for a list of traces",
        "P99 is always lower than P50 in this simulator"
      ],
      "correct": 1,
      "explanation": "A small number of long-prefill or queue-spike traces can dominate user experience while barely moving the mean; percentiles expose those tails."
    }
  ]
}
