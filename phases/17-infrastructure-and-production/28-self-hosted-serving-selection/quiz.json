{
  "lesson": "28-self-hosted-serving-selection",
  "title": "Self-Hosted Serving Selection — llama.cpp, Ollama, TGI, vLLM, SGLang",
  "questions": [
    {
      "stage": "pre",
      "question": "Which engine does the lesson pick as the dev-laptop one-command default?",
      "options": [
        "Ollama — one-command install, ~15-30% slower than llama.cpp, the dev-laptop default",
        "TGI — Hugging Face's engine, now in maintenance mode as of December 2025",
        "vLLM — the general-purpose production default, not the one-command dev-laptop pick",
        "llama.cpp — fastest on CPU with the widest model support, not the one-command dev-laptop pick"
      ],
      "correct": 0,
      "explanation": "The lesson positions Ollama as the dev-laptop one-command install for a single user, trading roughly 15-30% throughput versus llama.cpp for ease of setup -- it is not the pick for production scale."
    },
    {
      "stage": "check",
      "question": "What 2025 event changes the default away from TGI for new projects?",
      "options": [
        "TGI was acquired by Anthropic, a change in ownership rather than a support-status change",
        "TGI entered maintenance mode on December 11, 2025 — only bug fixes going forward",
        "TGI raised prices, a pricing change the lesson does not cite as the reason projects moved away",
        "TGI dropped CUDA support, a hardware-compatibility change the lesson does not describe happening"
      ],
      "correct": 1,
      "explanation": "The lesson states Hugging Face TGI entered maintenance mode on December 11, 2025 (bug fixes only), which makes it a risky long-term bet and biases new projects toward vLLM or SGLang instead."
    },
    {
      "stage": "check",
      "question": "Which hardware constraint forces llama.cpp and excludes vLLM / TRT-LLM?",
      "options": [
        "Apple M4 — Metal-accelerated, handled by llama.cpp but not the constraint that excludes vLLM and TRT-LLM",
        "AMD MI300X — supported by vLLM via ROCm, not a constraint that excludes vLLM",
        "NVIDIA Hopper — supported by vLLM, SGLang, and TRT-LLM, not a constraint that excludes any of them",
        "CPU only (no accelerator)"
      ],
      "correct": 3,
      "explanation": "The lesson's hardware-first decision tree states CPU-only deployments must use llama.cpp (Ollama also works but is slower) since vLLM and TRT-LLM require a GPU accelerator."
    },
    {
      "stage": "check",
      "question": "Which engine does the lesson position for agentic multi-turn and prefix-heavy workloads thanks to RadixAttention?",
      "options": [
        "SGLang — RadixAttention specialist for agentic multi-turn and prefix-heavy workloads, 400,000+ GPUs in production",
        "Ollama — the one-command dev-laptop engine, not the agentic multi-turn specialist",
        "llama.cpp — the CPU-first engine, not the agentic multi-turn specialist",
        "TGI — now in maintenance mode, not the agentic multi-turn specialist"
      ],
      "correct": 0,
      "explanation": "The lesson names SGLang as the agentic multi-turn and prefix-heavy specialist, crediting its RadixAttention mechanism and citing 400,000+ GPUs in production at companies like xAI, LinkedIn, and Cursor."
    },
    {
      "stage": "post",
      "question": "What dev-to-prod pipeline does the lesson recommend on the same GGUF or HF weights?",
      "options": [
        "Only TRT-LLM, top to bottom, skipping the dev/staging split the lesson recommends",
        "Ollama in dev and Ollama in prod, skipping the staging and production-engine handoff the lesson recommends",
        "Ollama in dev, llama.cpp in staging, vLLM (or SGLang for prefix-heavy) in prod",
        "TGI everywhere, using the same maintenance-mode engine across dev, staging, and prod"
      ],
      "correct": 2,
      "explanation": "The lesson's 2026 pipeline pattern is dev on Ollama, staging on llama.cpp, and production on vLLM (or SGLang for prefix-heavy workloads), all using the same GGUF or HF weights throughout."
    },
    {
      "stage": "post",
      "question": "Why is Ollama discouraged for shared production?",
      "options": [
        "It is closed source, a licensing concern the lesson does not raise about Ollama",
        "Go HTTP serialization adds overhead, concurrency management is simpler than vLLM, and OpenTelemetry support lags",
        "It only runs on Windows, a platform limitation the lesson does not attribute to Ollama",
        "It cannot load GGUF weights, a format limitation the lesson does not attribute to Ollama"
      ],
      "correct": 1,
      "explanation": "The lesson's Ollama caveat is that its Go HTTP serialization adds overhead, its concurrency management is simpler than vLLM's, and its OpenTelemetry support lags -- fine for one user, not for shared production."
    }
  ]
}
