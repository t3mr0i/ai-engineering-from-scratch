{
  "lesson": "10-cold-start-mitigation",
  "title": "Cold Start Mitigation for Serverless LLMs",
  "questions": [
    {
      "stage": "pre",
      "question": "What raw cold-start total does total_for_stack compute for the five PHASES_70B entries?",
      "options": [
        "328 seconds",
        "180 seconds",
        "75 seconds",
        "50 seconds"
      ],
      "correct": 0,
      "explanation": "The RAW path sums 50 + 180 + 75 + 20 + 3 = 328 seconds across node, image, HBM load, engine init, and first forward."
    },
    {
      "stage": "check",
      "question": "Which phase does pre_seeded eliminate in the local stack model?",
      "options": [
        "Node provision",
        "Image pull",
        "Engine initialization",
        "The first forward"
      ],
      "correct": 1,
      "explanation": "The image pull row has pre_seeded_sec=0; the node, weight, engine, and first-forward phases still retain their pre-seeded values."
    },
    {
      "stage": "check",
      "question": "What does the streamer option change for the weights-to-HBM phase?",
      "options": [
        "It raises 75 seconds to 180 seconds",
        "It removes the node-provision phase",
        "It replaces 75 seconds with 35 seconds",
        "It replaces all phases with a 0.5-second snapshot"
      ],
      "correct": 2,
      "explanation": "For weights to HBM, streamer_sec is 35 rather than the raw 75; other phases use their raw or pre-seeded values depending on the stack."
    },
    {
      "stage": "check",
      "question": "What total does the GPU_SNAPSHOT stack produce from the phase table?",
      "options": [
        "328 seconds",
        "148 seconds",
        "35 seconds",
        "3 seconds"
      ],
      "correct": 3,
      "explanation": "The snapshot values sum to 0.5 + 0 + 0 + 2 + 0.5 = 3 seconds, replacing the raw phase times."
    },
    {
      "stage": "post",
      "question": "What is the main warm-pool trade-off for an interactive path?",
      "options": [
        "Pay for an idle GPU",
        "Trade a larger model for a smaller tokenizer",
        "Use scale-to-zero and guarantee no first-request delay",
        "Move KV cache to a cheaper region"
      ],
      "correct": 0,
      "explanation": "min_workers=1 avoids the cold start on the warm path but carries a 24×7 GPU rental; the lesson recommends it for latency-sensitive chat or voice."
    },
    {
      "stage": "post",
      "question": "Why does live migration transfer input tokens rather than a full KV cache?",
      "options": [
        "Input tokens are larger than KV tensors",
        "Transfer KB tokens; recompute GB KV",
        "KV caches contain no model information",
        "The engine only supports images as migration data"
      ],
      "correct": 1,
      "explanation": "The lesson's design transfers a compact token sequence and pays recomputation, avoiding network movement of a GB-scale KV cache."
    }
  ]
}
