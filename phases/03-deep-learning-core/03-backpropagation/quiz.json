{
  "lesson": "03-backpropagation",
  "title": "Backpropagation from Scratch",
  "questions": [
    {
      "stage": "pre",
      "question": "For z=a+b, an upstream gradient dz/dz of 1 reaches the addition node. What gradient does the Value engine send to a?",
      "options": [
        "1",
        "0",
        "b",
        "a+b"
      ],
      "correct": 0,
      "explanation": "The local derivative of addition with respect to either operand is 1, so the upstream gradient passes through unchanged."
    },
    {
      "stage": "check",
      "question": "If z=a*b with a=3 and b=4, what is dz/da when the upstream gradient is 1?",
      "options": [
        "3",
        "4",
        "7",
        "12"
      ],
      "correct": 1,
      "explanation": "The product rule gives dz/da=b. With b=4 and upstream gradient 1, the local contribution is 4."
    },
    {
      "stage": "check",
      "question": "What is the derivative of sigmoid(z) at z=0 in the Value implementation?",
      "options": [
        "0",
        "0.5",
        "0.25",
        "1"
      ],
      "correct": 2,
      "explanation": "sigmoid(0)=0.5 and s(1-s)=0.5*0.5=0.25."
    },
    {
      "stage": "check",
      "question": "A graph contains y=x*x with x=3. After y.backward(), what should x.grad be?",
      "options": [
        "3",
        "9",
        "1",
        "6"
      ],
      "correct": 3,
      "explanation": "Both paths through the multiplication contribute x, so dy/dx=x+x=2x=6. The engine's += operations accumulate both paths."
    },
    {
      "stage": "post",
      "question": "Why does Network.zero_grad() run before backpropagating a new training objective?",
      "options": [
        "Leaf parameter gradients accumulate between objectives, so clear them before a fresh update",
        "It reinitializes all weights to zero before every example",
        "It changes sigmoid outputs into hard classes",
        "It makes the topological sort visit fewer nodes"
      ],
      "correct": 0,
      "explanation": "Leaf Values accumulate parameter gradients, so Network.zero_grad clears them between objectives. Intermediate adjoints are reset automatically at the start of each backward pass."
    },
    {
      "stage": "post",
      "question": "Why can a deep chain of sigmoid layers have vanishing gradients?",
      "options": [
        "Sigmoid derivatives are always greater than one",
        "Each local sigmoid derivative is at most 0.25, so multiplying many of them shrinks the signal",
        "Backpropagation never visits hidden layers",
        "The loss is computed before the forward pass"
      ],
      "correct": 1,
      "explanation": "The chain rule multiplies local derivatives. Repeated factors below one can make the gradient reaching early layers extremely small."
    }
  ]
}
