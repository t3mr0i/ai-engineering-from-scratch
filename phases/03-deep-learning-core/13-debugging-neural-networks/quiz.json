{
  "lesson": "13-debugging-neural-networks",
  "title": "Debugging Neural Networks with Evidence",
  "questions": [
    {
      "stage": "pre",
      "question": "What status does loss_health((1.0, 1.0, 1.0, 1.0)) return?",
      "options": [
        "HEALTHY, because four points are enough to certify a model",
        "NOT_DECREASING, because a constant short trace supplies no downward evidence",
        "NAN_OR_INF, because constant values are invalid",
        "NOT_ENOUGH_DATA, because only traces of 20 points are accepted"
      ],
      "correct": 1,
      "explanation": "For fewer than 20 points, loss_health compares the last value with the first; equality meets the 0.99 non-decrease rule, so a constant short trace is not called healthy."
    },
    {
      "stage": "check",
      "question": "What status does loss_health((1.0, float('nan'))) return?",
      "options": [
        "HEALTHY",
        "NOT_DECREASING",
        "NAN_OR_INF",
        "NOT_ENOUGH_DATA"
      ],
      "correct": 2,
      "explanation": "The non-finite scan runs after the minimum-length check and labels any NaN or infinity as NAN_OR_INF."
    },
    {
      "stage": "check",
      "question": "For activation_report('h', (0.0, 0.0, 0.0, 1.0)), which issue is reported?",
      "options": [
        "DEAD_NEURONS",
        "EXPLODING_ACTIVATIONS",
        "HEALTHY",
        "VANISHING_GRADIENT"
      ],
      "correct": 0,
      "explanation": "Three of four entries are zero, so the zero fraction is 0.75 and exceeds the helper's 0.5 dead-neuron threshold."
    },
    {
      "stage": "check",
      "question": "Which condition makes gradient_report classify a layer as VANISHING_GRADIENT?",
      "options": [
        "The activation maximum is above 10",
        "The gradient absolute mean is below 1e-7",
        "The loss history has fewer than two values",
        "The gradient absolute mean is above 100"
      ],
      "correct": 1,
      "explanation": "The gradient helper compares the mean absolute magnitude with its explicit small threshold of 1e-7."
    },
    {
      "stage": "post",
      "question": "What value should central_difference(lambda x: x*x, 3.0) approximate?",
      "options": [
        "3",
        "6",
        "9",
        "12"
      ],
      "correct": 1,
      "explanation": "The centered finite difference estimates the derivative of x²; at x=3, the analytic derivative 2x is 6."
    },
    {
      "stage": "post",
      "question": "What is the appropriate conclusion when torch_available() is false in the canonical demo?",
      "options": [
        "The diagnostic helpers cannot run at all",
        "A model reached a default accuracy",
        "The optional torch path was not executed, so report only the stdlib evidence",
        "Install torch from inside main.py before continuing"
      ],
      "correct": 2,
      "explanation": "The lesson's diagnostics are framework-independent. A missing optional backend is a boundary to report, not a reason to install packages or fabricate training evidence."
    }
  ]
}
