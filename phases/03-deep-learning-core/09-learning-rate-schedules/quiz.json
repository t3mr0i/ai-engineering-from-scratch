{
  "lesson": "09-learning-rate-schedules",
  "title": "Learning Rate Schedules and Warmup",
  "questions": [
    {
      "stage": "pre",
      "question": "What does constant_schedule(7, lr=0.03) return?",
      "options": [
        "0.03",
        "0.0",
        "0.03 / 7",
        "It raises because step is not zero"
      ],
      "correct": 0,
      "explanation": "The constant schedule validates the non-negative step and returns the positive learning rate unchanged."
    },
    {
      "stage": "check",
      "question": "For lr=0.1, step_size=4, and gamma=0.5, what is step_decay_schedule(8, ...)?",
      "options": [
        "0.05",
        "0.0125",
        "0.025",
        "0.1"
      ],
      "correct": 2,
      "explanation": "The bucket is 8 // 4 = 2, so the value is 0.1 * 0.5**2 = 0.025."
    },
    {
      "stage": "check",
      "question": "What are the two boundary values for cosine_schedule(step, lr=0.1, total_steps=10, lr_min=0.01)?",
      "options": [
        "0.01 at step 0 and 0.1 at step 10",
        "0.1 at step 0 and 0.01 at step 10 or later",
        "0.1 at both steps because cosine is periodic",
        "0.0 at step 0 and 0.1 at step 10"
      ],
      "correct": 1,
      "explanation": "Step zero is the cosine peak. At step 10 the implementation clamps to the requested floor, and later steps stay there."
    },
    {
      "stage": "check",
      "question": "With warmup_steps=3 and lr=0.1, what does warmup_cosine_schedule return at step 1 before decay begins?",
      "options": [
        "0.1 / 3",
        "0.1 / 2",
        "2 * 0.1 / 3",
        "lr_min regardless of the warmup"
      ],
      "correct": 2,
      "explanation": "Warmup uses (step+1)/warmup_steps, so step 1 returns 2/3 of the peak: 2*0.1/3."
    },
    {
      "stage": "post",
      "question": "Which one-cycle call samples the shortest valid start/peak/finish trace?",
      "options": [
        "schedule_values(one_cycle_schedule, 2, lr=0.1)",
        "schedule_values(one_cycle_schedule, 3, lr=0.1)",
        "schedule_values(one_cycle_schedule, 1, lr=0.1)",
        "schedule_values(one_cycle_schedule, 0, lr=0.1)"
      ],
      "correct": 1,
      "explanation": "The implementation requires total_steps >= 3. With three points the values are approximately 0.004, 0.1, and 0.00001; step 3 and later return the finish value."
    },
    {
      "stage": "post",
      "question": "Which call violates the warmup schedule's horizon contract?",
      "options": [
        "warmup_cosine_schedule(0, lr=0.1, total_steps=10, warmup_steps=0)",
        "warmup_cosine_schedule(2, lr=0.1, total_steps=10, warmup_steps=3)",
        "warmup_cosine_schedule(0, lr=0.1, total_steps=10, warmup_steps=10)",
        "warmup_cosine_schedule(9, lr=0.1, total_steps=10, warmup_steps=3)"
      ],
      "correct": 2,
      "explanation": "The ramp must be shorter than the total horizon. Equal warmup and total steps leaves no cosine segment, so the function raises ValueError."
    }
  ]
}
